# Agent System

!!! note "Work in Progress"
    This documentation is being written.

## ReAct Loop

The FraggleAgent implements a ReAct-style (Reasoning + Acting) loop that iteratively processes messages and executes tools until a final response is generated.
