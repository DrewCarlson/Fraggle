{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Fraggle","text":"<p>Fraggle is a Kotlin-based AI assistant framework that integrates with messaging platforms. It runs its own ReAct-style agent loop against local or cloud LLM providers and ships with extensible tools for tasks like file operations, web fetching, and command execution. It also ships a terminal coding agent, <code>fraggle code</code>, that reuses the same agent loop, tools, and compaction infrastructure for in-project pair-programming.</p>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li> <p> Chat Bridge Integration   Connect to messaging platforms like Signal and Discord, with support for direct conversations.</p> </li> <li> <p> Terminal Coding Agent <code>fraggle code</code> is a Mosaic-based TUI for pair-programming with an LLM in any project \u2014 JSONL session branching, <code>AGENTS.md</code> context files, Claude-Code-style edits.</p> </li> <li> <p> ReAct Agent Loop   Purpose-built reasoning and acting loop with streaming, tool dispatch, and iteration control for any OpenAI-compatible LLM.</p> </li> <li> <p> Extensible Tools   Type-safe <code>AgentToolDef</code> definitions backed by <code>kotlinx.serialization</code> make it easy to add custom capabilities.</p> </li> <li> <p> Persistent Memory   Hierarchical memory system with global, chat, and user scopes.</p> </li> <li> <p> Sandboxed Execution   File and shell operations run in a configurable sandbox environment.</p> </li> <li> <p> Multiple LLM Providers   Works with LM Studio, OpenAI, and Anthropic APIs.</p> </li> </ul>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code># Download latest release\nwget https://github.com/DrewCarlson/Fraggle/releases/latest/download/fraggle.zip\nunzip fraggle.zip\ncd fraggle\n\n# Run the configuration wizard\n./bin/fraggle configure\n\n# Start chatting\n./bin/fraggle chat\n\n# Or pair-program with the terminal coding agent\n./bin/fraggle code\n</code></pre> <p>See the Getting Started guide for full setup instructions.</p>"},{"location":"#project-structure","title":"Project Structure","text":"<pre><code>Fraggle/\n\u251c\u2500\u2500 fraggle-cli/            # CLI application (run / chat / code / configure / worker)\n\u251c\u2500\u2500 fraggle-assistant/      # Messenger assistant (FraggleAgent, memory, chat bridges, db)\n\u251c\u2500\u2500 fraggle-coding-agent/   # Terminal coding agent (TUI, sessions, coding tools)\n\u251c\u2500\u2500 fraggle-agent-core/     # Generic agent runtime (loop, tools, executor, tracing, compaction)\n\u251c\u2500\u2500 fraggle-llm/            # LLM provider (LMStudioProvider, ChatRequest/Response)\n\u251c\u2500\u2500 fraggle-signal/         # Signal messaging bridge\n\u251c\u2500\u2500 fraggle-discord/        # Discord bot bridge\n\u251c\u2500\u2500 fraggle-tools/          # Built-in tool implementations\n\u251c\u2500\u2500 fraggle-api/            # REST API server\n\u251c\u2500\u2500 fraggle-dashboard/      # Web dashboard (Compose for Web)\n\u251c\u2500\u2500 fraggle-common/         # Shared models (Kotlin Multiplatform)\n\u2514\u2500\u2500 libs/                   # Internal libraries\n    \u2514\u2500\u2500 documented-*        # Annotation processor for config docs\n</code></pre>"},{"location":"#how-it-works","title":"How It Works","text":"<ol> <li>Message Received - A message arrives via a chat bridge (or interactive mode)</li> <li>Agent Processing - The agent loop evaluates the message and available tools</li> <li>Tool Execution - The agent calls tools as needed (file ops, web fetch, etc.)</li> <li>Response Sent - The final response is delivered back to the user</li> </ol> <p>The agent continues reasoning and acting until it produces a final response or reaches the iteration limit.</p>"},{"location":"#license","title":"License","text":"<p>Fraggle is published under the MIT License.</p>"},{"location":"coding-agent/","title":"Coding Agent","text":"<p><code>fraggle code</code> is a terminal coding agent that runs a ReAct-style agent loop against a local or cloud LLM and gives it read/write access to your project. It sits alongside the messenger assistant (<code>fraggle run</code> / <code>fraggle chat</code>) and shares the same provider configuration, so one LM Studio setup powers both.</p> <p>The coding agent is built on the same agent-loop, compaction, and tool-supervision infrastructure as the messenger assistant, but with a different shape: project-scoped sessions as JSONL files with tree branching, <code>AGENTS.md</code> context files walked from the project root, and a terminal UI instead of a chat bridge.</p>"},{"location":"coding-agent/#quick-start","title":"Quick Start","text":"<pre><code># Start a fresh session in the current directory\nfraggle code\n\n# Ask a one-shot question with an initial prompt\nfraggle code \"what does src/main/kotlin/Main.kt do?\"\n\n# Include a file in the initial prompt\nfraggle code @README.md \"summarize this\"\n\n# Resume the most recent session for this project\nfraggle code -c\n\n# Disable tool approval prompts (auto-approve all tool calls)\nfraggle code --supervision none\n</code></pre> <p>The first time you run <code>fraggle code</code> in a project, a new session file is created under <code>$FRAGGLE_ROOT/coding/sessions/&lt;project-hash&gt;/&lt;uuid&gt;.jsonl</code>. Subsequent runs in the same directory can resume that session with <code>-c</code>/<code>--continue</code> or browse prior sessions with <code>-r</code>/<code>--resume</code>.</p>"},{"location":"coding-agent/#cli-reference","title":"CLI Reference","text":"<pre><code>fraggle code [&lt;options&gt;] [&lt;message&gt;]...\n</code></pre>"},{"location":"coding-agent/#session-options","title":"Session options","text":"Flag Description <code>-c</code>, <code>--continue</code> Resume the most recent session for the current project. <code>-r</code>, <code>--resume</code> Resume the most recent session (alias of <code>--continue</code>). <code>--session &lt;path&gt;</code> Open a specific session file by path. <code>--fork &lt;path&gt;</code> Fork a specific session file into a new session in the same project. <code>--no-session</code> Ephemeral mode \u2014 start fresh, don't chain off any existing session. <p>The session flags are mutually exclusive; passing more than one is an error.</p>"},{"location":"coding-agent/#model-and-workspace","title":"Model and workspace","text":"Flag Description <code>--model &lt;id&gt;</code> Override the model id from <code>fraggle.yaml</code> and <code>settings.json</code>. <code>--workdir &lt;path&gt;</code> Override the working directory. All tool calls and context-file walks start here."},{"location":"coding-agent/#tool-selection","title":"Tool selection","text":"Flag Description <code>--tools &lt;list&gt;</code> Comma-separated list of tools to enable. Example: <code>--tools read_file,grep,edit_file</code>. <code>--no-tools</code> Disable all built-in tools. The agent becomes pure conversation."},{"location":"coding-agent/#system-prompt","title":"System prompt","text":"Flag Description <code>--system-prompt &lt;text&gt;</code> Replace the default system prompt entirely with the given text. <code>--append-system-prompt &lt;text&gt;</code> Append text to whichever system prompt is active."},{"location":"coding-agent/#supervision","title":"Supervision","text":"Flag Description <code>--supervision &lt;ask\\|none&gt;</code> <code>ask</code> prompts for approval on every tool call (default). <code>none</code> auto-approves all tool calls \u2014 use this in containers or other sandboxed environments."},{"location":"coding-agent/#positional-arguments","title":"Positional arguments","text":"<p>Any text after the options becomes the initial user message. Arguments that start with <code>@</code> are treated as file paths and their contents are inlined into the message as a code block:</p> <pre><code>fraggle code @src/Foo.kt @src/Bar.kt \"these two files look similar \u2014 should they be merged?\"\n</code></pre>"},{"location":"coding-agent/#interactive-mode","title":"Interactive Mode","text":"<p>Once <code>fraggle code</code> launches the TUI, you get a live message list, a multi-line editor, and a status footer.</p>"},{"location":"coding-agent/#layout","title":"Layout","text":"<pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  fraggle code  \u2502  ?: /hotkeys  \u2502  3 context files  \u2502  ...   \u2502  \u2190 Header\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502  \u00bb what's in foo.kt?                                        \u2502\n\u2502                                                             \u2502\n\u2502  \u25c6 I'll read the file                                       \u2502\n\u2502    \u2514\u2500 read_file {\"path\":\"foo.kt\"}                           \u2502\n\u2502         \u2190 read_file: fun main() { println(\"hi\") }           \u2502\n\u2502  \u25c6 The file defines a main function that prints hello.      \u2502\n\u2502                                                             \u2502  \u2190 MessageList\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502  &gt; your next message here_                                  \u2502\n\u2502                                                             \u2502  \u2190 Editor\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502 ~/proj \u2502 8f3c1a \u2502 128 tok \u2502 2% ctx \u2502 idle  \u2502 qwen3-coder    \u2502  \u2190 Footer\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <ul> <li>\u00bb marks user messages.</li> <li>\u25c6 marks assistant turns. Tool calls appear as indented bullets beneath the assistant that emitted them.</li> <li>\u2190 marks successful tool results; \u2717 marks errors.</li> </ul>"},{"location":"coding-agent/#keyboard-shortcuts","title":"Keyboard shortcuts","text":"Key Action <code>Enter</code> Submit the current message. <code>Shift+Enter</code> Insert a newline without submitting. <code>Backspace</code> / <code>Delete</code> Delete character behind / at cursor. <code>\u2190</code> / <code>\u2192</code> Cursor left / right. <code>\u2191</code> / <code>\u2193</code> Move cursor up / down between lines (column preserved when possible). <code>Home</code> / <code>Ctrl+A</code> Jump to the start of the current logical line. <code>End</code> / <code>Ctrl+E</code> Jump to the end of the current logical line. <code>Ctrl+K</code> Delete from the cursor to the end of the current line. <code>Ctrl+U</code> Delete from the cursor to the start of the current line. <code>Ctrl+W</code> Delete the word immediately before the cursor. <code>Ctrl+H</code> Alias for <code>Backspace</code>. <code>Escape</code> If the agent is running: abort the current turn. If the editor has text: clear it. Otherwise: exit (pressing twice confirms). <code>Ctrl+C</code> Clear the editor if it has text, otherwise exit. <code>y</code> / <code>n</code> Approve / deny a pending tool call (only when the approval overlay is visible). <p>Slash commands and skill invocations are submitted via the editor. Typing <code>/hotkeys</code> shows this table in-session; <code>/session</code> prints the active session id and file path; <code>/skill:&lt;name&gt;</code> expands into the body of an installed skill (see Skills below); and <code>/quit</code> exits.</p>"},{"location":"coding-agent/#terminal-runtime","title":"Terminal runtime","text":"<p>The TUI is built on <code>fraggle-tui</code>, a bespoke terminal runtime in this repo. It uses a differential renderer on top of Mosaic's lower-level TTY and event layers (raw-mode handling, ANSI/Kitty protocol parsing, in-band resize detection), but owns its own rendering pipeline:</p> <ul> <li>Full-tree diff + line-level rewrite. Every frame, the whole component   tree renders to a flat line list. Only lines that changed between the   previous and new frame are written to the terminal. Appended rows flow   through <code>\\r\\n</code> so they scroll naturally into the terminal's native   scrollback.</li> <li>Synchronized output mode (DEC 2026) wraps every write when supported,   so partial frames never appear.</li> <li>Strict width contract. Any rendered line whose visible width exceeds   the current terminal width aborts the render with a diagnostic error \u2014   wrap mismatch is the single largest source of ghost/trail bugs in TUIs,   so it's caught at the source.</li> <li>Resize handling. On a resize event the runtime clears the viewport,   clears the scrollback, invalidates every component's width-dependent   caches, and redraws the full tree at the new width. This keeps scrollback   storage in lockstep with the on-screen frame instead of accumulating   duplicate copies of older wrap states.</li> </ul> <p>Tradeoff of the scrollback clear: any shell output that was in scrollback before you ran <code>fraggle code</code> (the prompt that launched the TUI, earlier commands) is wiped the first time you resize. Within-session history scrolls naturally back into scrollback as it overflows the viewport, so you can scroll up to see earlier messages without issue.</p>"},{"location":"coding-agent/#tool-approval-ask-mode","title":"Tool approval (ask mode)","text":"<p>When <code>supervision = ask</code> (the default), the TUI shows an approval overlay whenever the agent wants to run a tool:</p> <pre><code>\u256d\u2500\u2500\u2500 tool approval \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256e\n\u2502 tool: edit_file\n\u2502 args: {\"path\":\"src/Foo.kt\",\"old_string\":\"x\",\"new_string\":\"y\"}\n\u2502 approve? [y] yes   [n] no\n\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256f\n</code></pre> <p>Press <code>y</code> to let the tool call run or <code>n</code> (or <code>Escape</code>) to deny. The agent receives the denial as a tool error and usually asks you what to do instead.</p> <p>To run without prompts, start with <code>--supervision none</code> or set <code>\"supervision\": \"none\"</code> in <code>settings.json</code>.</p>"},{"location":"coding-agent/#sessions","title":"Sessions","text":"<p>Sessions are JSONL files with a tree structure. Every entry has an <code>id</code> and an optional <code>parentId</code>, so multiple branches can share a prefix. This is how forking works without creating new files for every branch point.</p>"},{"location":"coding-agent/#where-sessions-live","title":"Where sessions live","text":"<pre><code>$FRAGGLE_ROOT/coding/sessions/\n  &lt;project-hash&gt;/               # 12-char sha1 of the absolute project path\n    &lt;session-uuid&gt;.jsonl\n    &lt;session-uuid&gt;.jsonl\n</code></pre> <p>The project hash isolates each project's sessions so <code>fraggle code -r</code> in one directory can't show you sessions from another.</p>"},{"location":"coding-agent/#resuming","title":"Resuming","text":"<pre><code>fraggle code -c              # most recent session for this project\nfraggle code --session &lt;path&gt;  # specific session file\nfraggle code --fork &lt;path&gt;   # copy a session into a new file\nfraggle code --no-session    # ignore any existing sessions, start fresh\n</code></pre> <p>On resume, the agent loads the current branch from the session file and replays it as the initial conversation history. Tool calls, tool results, and token usage are all preserved.</p>"},{"location":"coding-agent/#jsonl-format","title":"JSONL format","text":"<p>Every line is a JSON object with:</p> <pre><code>{\n  \"id\": \"uuid\",\n  \"parentId\": \"uuid or null\",\n  \"timestampMs\": 1713000000000,\n  \"payload\": { \"kind\": \"user\" | \"assistant\" | \"tool_result\" | \"meta\" | \"root\", ... },\n  \"schemaVersion\": 1\n}\n</code></pre> <p>The first entry (<code>kind: root</code>) carries session metadata (<code>sessionId</code>, <code>projectRoot</code>, <code>model</code>, <code>createdAtMs</code>). Subsequent entries chain off it via <code>parentId</code>.</p> <p>You can inspect a session file directly with any JSON-aware tool (<code>jq</code>, <code>less</code>, an editor) \u2014 the format is designed to be human-readable.</p>"},{"location":"coding-agent/#compaction","title":"Compaction","text":"<p>Long sessions can exhaust the LLM's context window. When context usage crosses the compaction threshold (default 70%, configurable via <code>compactionTriggerRatio</code>), the agent:</p> <ol> <li>Summarizes the older half of the conversation via a dedicated LLM call.</li> <li>Replaces the in-memory history with <code>[synthetic summary message] + recent turns verbatim</code>.</li> <li>Writes a <code>Meta(label=\"compaction\")</code> entry to the session file with the full summary text.</li> </ol> <p>Compaction is lossy for the working conversation but the session file keeps the full original history.</p> <p>Tune via <code>settings.json</code>:</p> <pre><code>{\n  \"contextWindowTokens\": 32000,\n  \"compactionTriggerRatio\": 0.70,\n  \"compactionKeepRecentMessages\": 12\n}\n</code></pre> <p>When <code>contextWindowTokens</code> is 0 (unknown), the ratio-based policy silently disables itself and no proactive compaction happens.</p>"},{"location":"coding-agent/#context-files-agentsmd","title":"Context files (<code>AGENTS.md</code>)","text":"<p>The coding agent loads project-specific guidance from <code>AGENTS.md</code> files found in:</p> <ol> <li><code>$FRAGGLE_ROOT/coding/AGENTS.md</code> \u2014 global rules that apply to every project.</li> <li>Every directory from the project root (nearest <code>.git</code> ancestor) down to the current working directory.</li> </ol> <p>Files are concatenated into the system prompt in outer \u2192 inner order, so the nearest <code>AGENTS.md</code> to your <code>cwd</code> appears last and takes precedence semantically.</p> <p>If an <code>AGENTS.md</code> is absent in a given directory, the agent falls back to <code>CLAUDE.md</code> in that directory for compatibility with Claude Code-style projects.</p>"},{"location":"coding-agent/#example","title":"Example","text":"<pre><code>~/projects/webapp/\n\u251c\u2500\u2500 .git/\n\u251c\u2500\u2500 AGENTS.md              # root project rules\n\u251c\u2500\u2500 backend/\n\u2502   \u251c\u2500\u2500 AGENTS.md          # rules specific to backend work\n\u2502   \u2514\u2500\u2500 src/\n\u2514\u2500\u2500 frontend/\n    \u2514\u2500\u2500 src/\n</code></pre> <p>Running <code>fraggle code</code> from <code>~/projects/webapp/backend/src</code> picks up both <code>~/projects/webapp/AGENTS.md</code> and <code>~/projects/webapp/backend/AGENTS.md</code>, in that order.</p>"},{"location":"coding-agent/#system-prompt-layering","title":"System prompt layering","text":"<p>The final system prompt is composed in this order (top to bottom):</p> <ol> <li>Base prompt \u2014 the first one found from:</li> <li><code>--system-prompt &lt;text&gt;</code> flag</li> <li><code>&lt;cwd&gt;/.fraggle/coding/SYSTEM.md</code> (project override)</li> <li><code>$FRAGGLE_ROOT/coding/SYSTEM.md</code> (global override)</li> <li>bundled default (<code>CODING_SYSTEM.md</code> inside the jar)</li> <li>Workspace snapshot \u2014 cwd, git branch/HEAD/status (best-effort; skipped in non-git directories).</li> <li><code>AGENTS.md</code> content \u2014 global first, then outer-to-inner project files.</li> <li>Available templates \u2014 a list of <code>/name</code> entries from loaded prompt templates (see below).</li> <li>Append text \u2014 the concatenation of:</li> <li><code>&lt;cwd&gt;/.fraggle/coding/APPEND_SYSTEM.md</code> (project)</li> <li><code>$FRAGGLE_ROOT/coding/APPEND_SYSTEM.md</code> (global)</li> <li><code>--append-system-prompt &lt;text&gt;</code> flag</li> </ol> <p>Sections with no content are skipped entirely, so running in a non-git directory with no <code>AGENTS.md</code> produces a clean prompt without empty placeholder headers.</p>"},{"location":"coding-agent/#prompt-templates","title":"Prompt templates","text":"<p>Reusable prompt templates live as Markdown files in:</p> <ul> <li><code>$FRAGGLE_ROOT/coding/prompts/*.md</code> (global)</li> <li><code>&lt;cwd&gt;/.fraggle/coding/prompts/*.md</code> (project)</li> </ul> <p>Each file becomes a <code>/name</code> command where <code>name</code> is the filename without the extension (lowercased). Templates may contain <code>{{ variable }}</code> placeholders:</p> <pre><code>&lt;!-- ~/.fraggle/coding/prompts/review.md --&gt;\nReview {{ path }} focusing on:\n- thread safety\n- error handling\n- performance hot spots\n</code></pre> <p>The template is advertised to the model in the system prompt (so it knows <code>/review</code> is available) but not expanded until the user actually types <code>/review</code>. Variable substitution is literal; unknown variables are left as-is so you can see something went wrong.</p>"},{"location":"coding-agent/#tools","title":"Tools","text":"<p>The coding agent ships with these tools by default:</p> Tool Description <code>read_file</code> Read a file with optional line range. <code>write_file</code> Overwrite a file. Supervised by default. <code>append_file</code> Append to a file. Supervised by default. <code>edit_file</code> Exact-string replacement (Claude Code style). Requires <code>old_string</code> to match exactly once in the file unless <code>replace_all = true</code>. <code>list_files</code> <code>ls</code>-equivalent directory listing. <code>search_files</code> Glob-based file search. <code>file_exists</code> Check if a path exists. <code>delete_file</code> Delete a file. Supervised by default. <code>execute_command</code> Run a shell command in the project directory. Supervised by default. <code>fetch_webpage</code> HTTP GET with optional Playwright rendering. <code>fetch_api</code> HTTP request returning raw response. <code>get_current_time</code> Current time in any timezone."},{"location":"coding-agent/#edit_file-semantics","title":"<code>edit_file</code> semantics","text":"<p><code>edit_file</code> is the primary way the agent modifies source. It's an exact-string replacement:</p> <ul> <li><code>old_string</code> must match byte-for-byte, including whitespace and newlines.</li> <li>Zero matches \u2192 error asking the agent to re-read the file.</li> <li>Multiple matches without <code>replace_all</code> \u2192 error asking for more surrounding context to disambiguate.</li> <li>Multiple matches with <code>replace_all: true</code> \u2192 every occurrence replaced.</li> <li><code>old_string == new_string</code> \u2192 no-op with a clear message.</li> </ul> <p>This is intentionally strict: a model that's careless about context will produce errors the user sees, rather than silent corruption.</p>"},{"location":"coding-agent/#disabling-tools","title":"Disabling tools","text":"<pre><code># Only read-only tools\nfraggle code --tools read_file,search_files,list_files,grep\n\n# No tools at all\nfraggle code --no-tools\n</code></pre>"},{"location":"coding-agent/#skills","title":"Skills","text":"<p>Skills are reusable prompt-plus-code bundles that ship their own instructions and, optionally, Python scripts and encrypted environment variables. A skill lives as a directory containing a <code>SKILL.md</code> file with YAML frontmatter and a markdown body.</p> <pre><code>~/.fraggle/skills/code-review/\n\u251c\u2500\u2500 SKILL.md\n\u251c\u2500\u2500 requirements.txt      # optional Python deps; auto-installed into a venv\n\u2514\u2500\u2500 scripts/\n    \u2514\u2500\u2500 review.py\n</code></pre> <p>Skills are discovered from three locations, highest precedence first:</p> <ol> <li><code>--skill &lt;path&gt;</code> CLI flag or <code>skills.extra_paths</code> in <code>fraggle.yaml</code>    (explicit).</li> <li><code>&lt;cwd&gt;/.fraggle/skills/</code> (project).</li> <li><code>$FRAGGLE_ROOT/skills/</code> (global).</li> </ol> <p>When skills are enabled (the default), the coding agent injects a skill catalog into the system prompt so the model knows which skills are available by name + description. The full body of a skill is only read on demand \u2014 the model can request it with <code>read_file</code>, or the user can invoke it directly via <code>/skill:&lt;name&gt;</code>. Typing <code>/skill:code-review src/Foo.kt</code> expands to the SKILL.md body followed by the extra argument text and submits that as a single message.</p> <p>Skills that declare Python dependencies get their own virtual environment at <code>$FRAGGLE_ROOT/venvs/&lt;skill&gt;/</code>. When the agent runs <code>execute_command</code> with <code>skill = \"&lt;name&gt;\"</code>, the runtime activates that venv and injects any configured secrets as environment variables before execution. Secrets are stored at <code>$FRAGGLE_ROOT/secrets/&lt;skill&gt;/&lt;VAR&gt;</code> with restrictive POSIX permissions (700/600) and never pass through the model.</p> <p>Manage skills from the CLI:</p> <pre><code>fraggle skills list                         # show installed skills + source\nfraggle skills add &lt;path-or-url&gt;            # install a skill from disk or git\nfraggle skills remove &lt;name&gt;                # delete a skill\nfraggle skills validate                     # lint every SKILL.md\nfraggle skills setup &lt;name&gt;                 # (re)build the Python venv\nfraggle skills secrets set &lt;name&gt; &lt;VAR&gt;     # store a secret\nfraggle skills secrets list &lt;name&gt;          # list configured secret names\n</code></pre> <p>To disable skills entirely, set <code>skills.enabled: false</code> in <code>fraggle.yaml</code>.</p>"},{"location":"coding-agent/#markdown-rendering","title":"Markdown rendering","text":"<p>LLM output rendered in the TUI is parsed by the JetBrains CommonMark parser (GFM flavour) and emitted as ANSI-styled lines. Block handling:</p> <ul> <li>Headings render as bold text with the accent color. Level 1 adds   reverse-video; level 2 adds underline. The leading <code>#</code> characters are   not rendered \u2014 terminals don't need the syntax markers to communicate   hierarchy.</li> <li>Code blocks are drawn with a <code>\u250c\u2500 lang \u2500\u2500\u2510 / \u2502 \u2026 / \u2514\u2500</code> border in the   dim theme color. Content is hard-wrapped to fit inside the gutter.</li> <li>Lists (unordered + ordered) wrap body text under the bullet with a   matching continuation indent.</li> <li>Blockquotes use a <code>\u2502</code> gutter in quote-accent color with italic body   text.</li> <li>Horizontal rules render as <code>\u2500\u2500\u2500\u2500\u2500\u2500</code>.</li> <li>Inline bold / italic / strikethrough / inline-code / links are   preserved via ANSI styling.</li> </ul>"},{"location":"coding-agent/#latex-math","title":"LaTeX math","text":"<p>LLMs routinely emit math fragments like <code>$\\approx 70\\%$</code> or <code>$\\sum_{i=1}^{n} x_i$</code>. These are pre-processed before markdown parsing:</p> <ul> <li><code>$\u2026$</code> and <code>$$\u2026$$</code> pairs are unwrapped when the inner content contains   LaTeX markers (a <code>\\command</code>, <code>^</code>, or <code>_</code>). Bare currency mentions like   <code>$5 to $10</code> are left alone.</li> <li>Common commands are substituted with Unicode equivalents: <code>\\approx</code> \u2192 \u2248,   <code>\\leq</code> \u2192 \u2264, <code>\\pm</code> \u2192 \u00b1, <code>\\infty</code> \u2192 \u221e, all Greek letters (<code>\\alpha</code> \u2192 \u03b1,   <code>\\Pi</code> \u2192 \u03a0, etc.), arrows, set/logic operators. Escaped punctuation   (<code>\\%</code> \u2192 %, <code>\\$</code> \u2192 $, <code>\\{</code> \u2192 {) is unescaped.</li> <li>Unknown commands pass through as literal text \u2014 the user at least sees a   readable approximation instead of garbled TeX.</li> </ul> <p>The substitution is not a full LaTeX renderer; complex expressions (nested fractions, matrices, integrals with bounds) survive as their command form.</p>"},{"location":"coding-agent/#configuration","title":"Configuration","text":""},{"location":"coding-agent/#shared-config-fraggle_rootconfigfraggleyaml","title":"Shared config: <code>$FRAGGLE_ROOT/config/fraggle.yaml</code>","text":"<p>The LLM provider is configured once for all Fraggle apps:</p> <pre><code>fraggle:\n  provider:\n    type: lmstudio\n    url: http://localhost:1234/v1\n    model: qwen3-coder-30b-a3b\n    api_key: null\n</code></pre> <p><code>fraggle code</code>, <code>fraggle run</code>, and <code>fraggle chat</code> all read the same <code>provider</code> section.</p>"},{"location":"coding-agent/#coding-agent-settings-fraggle_rootcodingsettingsjson","title":"Coding-agent settings: <code>$FRAGGLE_ROOT/coding/settings.json</code>","text":"<p>Coding-agent-specific behaviour lives in a JSON file:</p> <pre><code>{\n  \"supervision\": \"ask\",\n  \"contextWindowTokens\": 32000,\n  \"compactionTriggerRatio\": 0.70,\n  \"compactionKeepRecentMessages\": 12,\n  \"maxIterations\": 20,\n  \"model\": null\n}\n</code></pre> <p>All fields are optional. Missing fields fall back to built-in defaults. Unknown fields are ignored so settings files forward-compatibly survive Fraggle upgrades.</p>"},{"location":"coding-agent/#project-overrides-cwdfragglecodingsettingsjson","title":"Project overrides: <code>&lt;cwd&gt;/.fraggle/coding/settings.json</code>","text":"<p>Drop a <code>settings.json</code> in your project's <code>.fraggle/coding/</code> directory to override globals for that project only. Project values win, field by field. Fields you don't set fall through to the global settings, then to the defaults.</p>"},{"location":"coding-agent/#cli-overrides","title":"CLI overrides","text":"<p>Command-line flags win over every file:</p> <pre><code>defaults  \u2192  global settings.json  \u2192  project settings.json  \u2192  CLI flags\n</code></pre>"},{"location":"coding-agent/#directory-layout-summary","title":"Directory layout summary","text":"<pre><code>$FRAGGLE_ROOT/\n\u251c\u2500\u2500 config/\n\u2502   \u2514\u2500\u2500 fraggle.yaml               # shared provider config\n\u251c\u2500\u2500 data/                          # messenger assistant (memory, db)\n\u2514\u2500\u2500 coding/\n    \u251c\u2500\u2500 settings.json              # global coding-agent settings\n    \u251c\u2500\u2500 AGENTS.md                  # global AGENTS.md\n    \u251c\u2500\u2500 SYSTEM.md                  # optional global system prompt override\n    \u251c\u2500\u2500 APPEND_SYSTEM.md           # optional global append\n    \u251c\u2500\u2500 prompts/                   # global /name templates\n    \u2502   \u251c\u2500\u2500 review.md\n    \u2502   \u2514\u2500\u2500 refactor.md\n    \u2514\u2500\u2500 sessions/\n        \u2514\u2500\u2500 &lt;project-hash&gt;/\n            \u251c\u2500\u2500 &lt;uuid&gt;.jsonl\n            \u2514\u2500\u2500 &lt;uuid&gt;.jsonl\n\n&lt;project-root&gt;/\n\u2514\u2500\u2500 .fraggle/\n    \u2514\u2500\u2500 coding/\n        \u251c\u2500\u2500 settings.json          # project-specific settings\n        \u251c\u2500\u2500 SYSTEM.md              # project system prompt override\n        \u251c\u2500\u2500 APPEND_SYSTEM.md       # project append\n        \u2514\u2500\u2500 prompts/               # project-specific templates\n</code></pre>"},{"location":"coding-agent/#troubleshooting","title":"Troubleshooting","text":""},{"location":"coding-agent/#no-model-configured","title":"\"No model configured\"","text":"<p>Set <code>provider.model</code> in <code>fraggle.yaml</code>, or pass <code>--model &lt;id&gt;</code> on the command line, or set <code>\"model\": \"...\"</code> in your coding-agent <code>settings.json</code>.</p>"},{"location":"coding-agent/#the-editor-feels-unresponsive","title":"The editor feels unresponsive","text":"<p>The TUI locks out typing while the agent is processing a turn. Watch the footer status \u2014 <code>thinking...</code> means the agent is running; <code>idle</code> means the editor will accept input. If the agent is stuck, press <code>Escape</code> to abort the current turn.</p>"},{"location":"coding-agent/#compaction-fires-too-often-not-often-enough","title":"Compaction fires too often / not often enough","text":"<p>Compaction is gated on both the policy AND the context-usage ratio. If you haven't set <code>contextWindowTokens</code> in <code>settings.json</code>, the ratio is always 0.0 and compaction never fires proactively. Set it to your model's actual context window size (e.g., <code>32000</code> for a 32K-context model) to enable ratio-based compaction.</p> <p>To disable compaction entirely, set <code>compactionKeepRecentMessages</code> larger than any session you expect.</p>"},{"location":"coding-agent/#i-edited-an-agentsmd-and-the-agent-doesnt-see-the-change","title":"I edited an <code>AGENTS.md</code> and the agent doesn't see the change","text":"<p><code>AGENTS.md</code> files are loaded at startup. Exit with <code>Escape</code> and run <code>fraggle code -c</code> to pick up the change.</p>"},{"location":"coding-agent/#session-file-is-corrupt","title":"Session file is corrupt","text":"<p>Malformed JSONL lines surface a clear error with the offending line number when the session is loaded. You can manually edit the file to fix the bad line, or delete the whole session file and start fresh \u2014 the coding agent never rewrites past entries, so edits and truncations are safe.</p>"},{"location":"coding-agent/#see-also","title":"See also","text":"<ul> <li>Installation</li> <li>Configuration reference</li> <li>Architecture overview</li> <li>Agent system (shared with messenger assistant)</li> <li>Tools reference</li> </ul>"},{"location":"architecture/agent/","title":"Agent System","text":"<p><code>FraggleAgent</code> orchestrates message processing by constructing a stateful <code>Agent</code> (<code>fraggle.agent.Agent</code>) that runs a ReAct-style (Reasoning + Acting) loop for reliable tool use with LLMs.</p>"},{"location":"architecture/agent/#agent-loop","title":"Agent Loop","text":"<p>The agent loop lives in <code>fraggle-agent-core/src/main/kotlin/fraggle/agent/loop/</code> (<code>runAgentLoop</code>, <code>AgentOptions</code>, <code>LlmBridge</code>, <code>ProviderLlmBridge</code>, <code>ToolCallExecutor</code>). It sends context to the LLM via an <code>LlmBridge</code>, dispatches tool calls, and iterates until a final response is produced. <code>FraggleAgent</code> is responsible for building the per-request input, wiring the bridge + registry, and managing memory.</p> <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502                  User Message                \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                       \u2502\n                       v\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502            Build Input                       \u2502\n\u2502  - Platform context (bridge info)            \u2502\n\u2502  - Memory context (global, chat, user)       \u2502\n\u2502  - Conversation history                      \u2502\n\u2502  - Current message                           \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                       \u2502\n                       v\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502       Agent + runAgentLoop                   \u2502\n\u2502  ReAct loop via LlmBridge until done         \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                       \u2502\n                       v\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502       Auto-Memory Extraction                 \u2502\n\u2502  (if enabled) Extract &amp; reconcile facts      \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                       \u2502\n                       v\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502            Return Response                   \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"architecture/agent/#iteration-limits","title":"Iteration Limits","text":"<p>The agent has a configurable maximum iteration count (<code>max_iterations</code>) to prevent infinite loops. Each tool call counts as one iteration.</p> <pre><code>fraggle:\n  agent:\n    max_iterations: 10  # Default\n</code></pre> <p>When the limit is reached, the agent forces a final response from the LLM.</p>"},{"location":"architecture/agent/#system-prompt-construction","title":"System Prompt Construction","text":"<p>The system prompt is built from template files and set once at agent construction time:</p> <ol> <li>SYSTEM.md - Core instructions and capabilities</li> <li>IDENTITY.md - Agent personality and behavior</li> <li>USER.md - User-specific instructions</li> </ol> <p>Tool definitions are registered in a <code>FraggleToolRegistry</code>, which generates JSON schemas from each tool's <code>kotlinx.serialization</code> descriptor and passes them to the LLM alongside the system prompt on every call.</p> <p>Files are loaded from <code>prompts_dir</code> and concatenated with separators.</p>"},{"location":"architecture/agent/#per-request-input","title":"Per-Request Input","text":"<p>Each message is processed with a per-request input string that includes:</p> <ol> <li>Platform context - Which bridge (Signal, Discord, etc.) and its capabilities</li> <li>Memory context - Relevant facts from Global, Chat, and User scopes</li> <li>Conversation history - Recent messages (configurable via <code>max_history_messages</code>)</li> <li>Current message - The user's message text</li> </ol>"},{"location":"architecture/agent/#conversation-history","title":"Conversation History","text":"<pre><code>fraggle:\n  agent:\n    max_history_messages: 20  # Messages to include\n</code></pre> <p>History is pruned to fit within the context window while preserving recent exchanges.</p>"},{"location":"architecture/agent/#error-handling","title":"Error Handling","text":"<p>When a tool execution fails:</p> <ol> <li>The error is formatted as a tool result</li> <li>The LLM receives the error and can retry or adapt</li> <li>After repeated failures, the agent may explain the issue to the user</li> </ol>"},{"location":"architecture/agent/#temperature-and-sampling","title":"Temperature and Sampling","text":"<p>Control LLM response randomness:</p> <pre><code>fraggle:\n  agent:\n    temperature: 0.7    # 0.0 = deterministic, 2.0 = very random\n    max_tokens: 4096    # Maximum response length\n</code></pre>"},{"location":"architecture/agent/#attachments","title":"Attachments","text":"<p>Some tools (like <code>screenshot_page</code>) produce attachments rather than text responses. Tools collect attachments via <code>ToolExecutionContext</code>, and the agent includes them alongside the text response when sending back through the bridge.</p>"},{"location":"architecture/memory/","title":"Memory System","text":"<p>Fraggle includes a hierarchical memory system that persists facts across conversations. Memory is stored as human-readable markdown and loaded directly by <code>FraggleAgent</code> when building per-request input for the agent loop.</p>"},{"location":"architecture/memory/#memory-scopes","title":"Memory Scopes","text":"<p>Memory is organized into three scopes:</p>"},{"location":"architecture/memory/#global-memory","title":"Global Memory","text":"<p>Shared across all conversations and users. Useful for:</p> <ul> <li>General knowledge the agent should always have</li> <li>Configuration or preferences that apply universally</li> <li>Facts learned that benefit all users</li> </ul> <p>Storage path: <code>$FRAGGLE_ROOT/data/memory/global.md</code></p>"},{"location":"architecture/memory/#chat-memory","title":"Chat Memory","text":"<p>Per-conversation memory. Useful for:</p> <ul> <li>Context about the specific chat or group</li> <li>Ongoing project information</li> <li>Chat-specific preferences</li> </ul> <p>Storage path: <code>$FRAGGLE_ROOT/data/memory/chats/{chat_id}/memory.md</code></p>"},{"location":"architecture/memory/#user-memory","title":"User Memory","text":"<p>Per-user memory that persists across chats. Useful for:</p> <ul> <li>User preferences and settings</li> <li>Personal information the user has shared</li> <li>History of user-specific interactions</li> </ul> <p>Storage path: <code>$FRAGGLE_ROOT/data/memory/users/{user_id}/memory.md</code></p>"},{"location":"architecture/memory/#storage-format","title":"Storage Format","text":"<p>Each memory file is a markdown list of facts with timestamps:</p> <pre><code># Memory\n\n- Name: Alice [created: 2025-01-15T10:30:00Z]\n- Hobbies: guitar, programming [created: 2025-01-15T10:30:00Z, updated: 2025-02-07T14:00:00Z]\n- Lives in: Berlin [created: 2025-02-01T08:00:00Z]\n</code></pre> <p>Facts use a concise <code>Key: Value</code> format. Each fact tracks when it was created and, if modified, when it was last updated.</p>"},{"location":"architecture/memory/#automatic-memory","title":"Automatic Memory","text":"<p>When <code>auto_memory</code> is enabled (the default), the agent automatically extracts and manages facts after each conversation exchange:</p> <ol> <li>Extraction - The LLM identifies new personal facts from the exchange, using concise <code>Key: Value</code> format and avoiding duplicates of already-stored facts</li> <li>Reconciliation - If existing facts are present, the LLM reconciles new facts with old ones: merging related facts, updating superseded information, preserving history (e.g., \"Previously worked at: Google\"), and removing duplicates</li> <li>Persistence - Facts are saved to the appropriate scope file with timestamps</li> </ol> <p>This means the agent builds up knowledge about users over time without explicit \"remember this\" commands.</p>"},{"location":"architecture/memory/#memory-in-context","title":"Memory in Context","text":"<p>All three memory scopes are loaded and included in the agent's input for each request, giving the agent awareness of:</p> <ul> <li>Global knowledge</li> <li>Chat-specific context</li> <li>User preferences and history</li> </ul>"},{"location":"architecture/memory/#configuration","title":"Configuration","text":"<pre><code>fraggle:\n  memory:\n    base_dir: ./data/memory\n  agent:\n    auto_memory: true  # Enable automatic fact extraction\n</code></pre> Option Description Default <code>base_dir</code> Directory where memory files are stored <code>./data/memory</code> <code>auto_memory</code> Automatically extract facts from conversations <code>true</code>"},{"location":"architecture/memory/#dashboard","title":"Dashboard","text":"<p>The web dashboard provides a memory management interface where you can:</p> <ul> <li>Browse all memory scopes and their fact counts</li> <li>Search and filter facts</li> <li>Edit individual facts inline</li> <li>Delete individual facts</li> <li>Clear all facts in a scope</li> </ul>"},{"location":"architecture/memory/#best-practices","title":"Best Practices","text":"<ol> <li>Be Specific - The agent stores specific facts in <code>Key: Value</code> format, not vague summaries</li> <li>Scope Appropriately - User-specific facts go in User scope, project context in Chat scope</li> <li>Review Periodically - Memory files are human-readable markdown and can be manually edited</li> <li>Fact Reconciliation - The agent merges related facts automatically (e.g., two hobby mentions become one combined list)</li> </ol>"},{"location":"architecture/overview/","title":"Architecture Overview","text":"<p>Fraggle is built as a modular Kotlin application with clear separation of concerns. The generic agent loop and tool system live in <code>fraggle-agent-core</code>, the messenger assistant that drives them lives in <code>fraggle-assistant</code>, and <code>fraggle-llm</code> hosts <code>LMStudioProvider</code> for OpenAI-compatible LLM traffic.</p>"},{"location":"architecture/overview/#module-structure","title":"Module Structure","text":"<pre><code>Fraggle/\n\u251c\u2500\u2500 fraggle-cli/            # CLI application and entry point\n\u251c\u2500\u2500 fraggle-assistant/      # Messenger assistant (FraggleAgent, memory, chat bridges, db)\n\u251c\u2500\u2500 fraggle-agent-core/     # Generic agent runtime (loop, tools, executor, tracing)\n\u251c\u2500\u2500 fraggle-llm/            # LLM provider (LMStudioProvider, ChatRequest/Response)\n\u251c\u2500\u2500 fraggle-signal/         # Signal messenger bridge\n\u251c\u2500\u2500 fraggle-discord/        # Discord bot bridge\n\u251c\u2500\u2500 fraggle-tools/          # Built-in tool implementations\n\u251c\u2500\u2500 fraggle-common/         # Shared models (Kotlin Multiplatform)\n\u251c\u2500\u2500 fraggle-api/            # REST API server\n\u251c\u2500\u2500 fraggle-dashboard/      # Web dashboard (Compose for Web)\n\u2514\u2500\u2500 libs/                   # Internal libraries\n    \u2514\u2500\u2500 documented-*        # Annotation processor for config docs\n</code></pre>"},{"location":"architecture/overview/#core-components","title":"Core Components","text":""},{"location":"architecture/overview/#fraggleagent","title":"FraggleAgent","text":"<p>The central component that orchestrates message processing. It constructs a stateful <code>Agent</code> that runs a ReAct (Reasoning + Acting) loop:</p> <ol> <li>Receives a message</li> <li>Builds per-request input (platform context, memory, conversation history)</li> <li>Constructs an <code>Agent</code> with the system prompt, tool definitions, and an <code>LlmBridge</code></li> <li>The agent loop sends context + tools to the LLM via <code>ProviderLlmBridge</code> \u2192 <code>LMStudioProvider</code>, dispatches tool calls, and iterates until a final response is produced</li> <li>If auto-memory is enabled, new facts are extracted and reconciled</li> </ol> <p>See Agent System for details.</p>"},{"location":"architecture/overview/#tool-system","title":"Tool System","text":"<p>Tools are the capabilities available to the agent. Each tool extends <code>AgentToolDef</code> with type-safe serializable parameters:</p> <pre><code>class MyTool : AgentToolDef&lt;MyTool.Args&gt;(\n    name = \"my_tool\",\n    description = \"Does something useful\",\n    argsSerializer = Args.serializer(),\n) {\n    @Serializable\n    data class Args(\n        @LLMDescription(\"The input value\")\n        val input: String,\n    )\n    override suspend fun execute(args: Args): String = \"Result: ${args.input}\"\n}\n</code></pre> <p>See Tools for the full reference.</p>"},{"location":"architecture/overview/#memory-system","title":"Memory System","text":"<p>Hierarchical memory storage with three scopes:</p> <ul> <li>Global - Shared across all conversations</li> <li>Chat - Per-conversation memory</li> <li>User - Per-user memory across chats</li> </ul> <p>Memory is persisted as human-readable markdown files. <code>FraggleAgent</code> reads directly from <code>MemoryStore</code> when building per-request input.</p> <p>See Memory for details.</p>"},{"location":"architecture/overview/#tool-execution","title":"Tool Execution","text":"<p>The executor system controls how tools run, with two dimensions of configuration:</p> <p>Execution mode \u2014 Tools run either locally in the Fraggle process (<code>local</code>) or are forwarded to a separate worker process over HTTP (<code>remote</code>). Remote execution isolates tool I/O from the main agent.</p> <p>Supervision \u2014 In <code>supervised</code> mode, each tool call is evaluated against policy-based rules before it runs. Rules can specify <code>allow</code>, <code>deny</code>, or <code>ask</code> policies with optional argument pattern constraints (e.g., deny <code>write_file</code> for paths under <code>/etc/**</code>). Path arguments are normalized to prevent traversal attacks. When no rule matches or the policy is <code>ask</code>, approval is requested interactively \u2014 on stdin in the CLI (<code>fraggle chat</code>) or via a WebSocket permission event in production (<code>fraggle run</code>).</p> <p>Every tool that performs I/O (file, shell, web) is wrapped in a <code>ManagedTool</code> that checks supervision and optionally forwards to a remote worker. Scheduling tools are not wrapped since they don't perform external I/O.</p>"},{"location":"architecture/overview/#data-flow","title":"Data Flow","text":"<pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510     \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510     \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510     \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502    Chat     \u2502\u2500\u2500\u2500\u2500&gt;\u2502   Agent     \u2502\u2500\u2500\u2500\u2500&gt;\u2502 ManagedTool \u2502\u2500\u2500\u2500\u2500&gt;\u2502   Worker    \u2502\n\u2502   Bridge    \u2502&lt;\u2500\u2500\u2500\u2500\u2502    Loop     \u2502&lt;\u2500\u2500\u2500\u2500\u2502 (supervise) \u2502     \u2502  (remote)   \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518     \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518     \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518     \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                          \u2502                    \u2502\n                          v                    v\n                    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510     \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                    \u2502   Memory    \u2502     \u2502  Local I/O  \u2502\n                    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518     \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <ol> <li>Chat Bridge receives messages and passes them to the agent</li> <li>Agent builds context and runs the agent loop, calling tools as needed</li> <li>ManagedTool checks supervision (evaluate tool policies or prompt for permission), then either executes locally or forwards to the remote worker</li> <li>Memory stores and retrieves conversation context</li> <li>Agent generates final response</li> <li>Chat Bridge sends the response back to the user</li> </ol>"},{"location":"architecture/overview/#configuration","title":"Configuration","text":"<p>Configuration is managed through a YAML file with type-safe Kotlin models:</p> <pre><code>@Serializable\ndata class FraggleConfig(\n    val fraggle: FraggleSettings,\n)\n</code></pre> <p>The <code>@Documented</code> annotation generates runtime documentation that powers the web dashboard's settings UI.</p>"},{"location":"architecture/overview/#platform-support","title":"Platform Support","text":"<p>Fraggle uses Kotlin Multiplatform for shared code:</p> <ul> <li>JVM - Main application, chat bridges, tools</li> <li>JS - Web dashboard (Compose for Web)</li> </ul> <p>Shared modules (like configuration models) compile to both targets.</p>"},{"location":"architecture/skills/","title":"Agent Skills","text":"<p>Skills are self-contained capability bundles the Fraggle agent can discover and invoke. A skill packages a workflow \u2014 step-by-step instructions, scripts, reference material \u2014 into a directory containing a <code>SKILL.md</code> file with YAML frontmatter. Skills are portable: Fraggle follows the agentskills.io specification, so a skill authored for Fraggle also works with other agent platforms that implement the spec (and vice versa).</p>"},{"location":"architecture/skills/#how-they-work","title":"How they work","text":"<p>Skills use progressive disclosure: only skill metadata (name, description, path) is injected into the system prompt at startup. The model reads the full <code>SKILL.md</code> body on demand via the built-in <code>read_file</code> tool when a user request matches a skill's description. This keeps the context window small no matter how many skills you install.</p> <p>The model never runs a skill in a sandbox \u2014 a skill is just a markdown document with instructions the model chooses to follow. Any side effects (running scripts, editing files, shell commands) happen through the normal tool system under the same supervision rules as any other tool call.</p>"},{"location":"architecture/skills/#skill-layout","title":"Skill layout","text":"<p>A skill is a directory containing at minimum a <code>SKILL.md</code> file:</p> <pre><code>config/skills/\n\u251c\u2500\u2500 code-review/\n\u2502   \u251c\u2500\u2500 SKILL.md\n\u2502   \u251c\u2500\u2500 scripts/\n\u2502   \u2502   \u2514\u2500\u2500 check-style.sh\n\u2502   \u2514\u2500\u2500 references/\n\u2502       \u2514\u2500\u2500 style-guide.md\n\u2514\u2500\u2500 commit-message/\n    \u2514\u2500\u2500 SKILL.md\n</code></pre> <p>The directory name must match the <code>name</code> field in the frontmatter.</p>"},{"location":"architecture/skills/#skillmd-format","title":"SKILL.md format","text":"<pre><code>---\nname: code-review\ndescription: Review a pull request for correctness, style, and potential bugs.\nlicense: MIT\n---\n\n# Code Review\n\nWhen the user asks for a code review:\n\n1. Read the diff with `git diff main...HEAD`.\n2. For each changed file, read the full file to understand context.\n3. Check for:\n   - Missing error handling\n   - Inconsistent naming\n   - Tests for new behavior\n4. Report findings as a bulleted list.\n\nStyle rules for this repo are in `references/style-guide.md` (relative to this skill).\n</code></pre>"},{"location":"architecture/skills/#required-frontmatter-fields","title":"Required frontmatter fields","text":"Field Type Rules <code>name</code> string 1\u201364 chars, lowercase <code>a\u2013z</code>, digits, hyphens only; must match parent directory name <code>description</code> string 1\u20131024 chars. Describes what the skill does and when to use it. Shown to the model."},{"location":"architecture/skills/#optional-frontmatter-fields","title":"Optional frontmatter fields","text":"Field Type Meaning <code>license</code> string SPDX identifier. Metadata only. <code>compatibility</code> string Agent platform compatibility hint. Metadata only. <code>allowed-tools</code> string list Declared tool allowlist (parsed but not enforced in the current release). <code>env</code> string list Environment variable names the skill requires (API keys, credentials). See Secrets. <code>disable-model-invocation</code> boolean If <code>true</code>, hide the skill from the auto-catalog. Still reachable via <code>/skill:name</code> explicitly. <p>Validation is lenient: bad names or over-long descriptions produce a warning in the logs but the skill still loads. Missing <code>description</code> is the only hard failure \u2014 the skill is skipped.</p>"},{"location":"architecture/skills/#discovery","title":"Discovery","text":"<p>At startup, Fraggle scans these locations in order and builds a combined registry:</p> Source Path Precedence (higher wins) <code>PACKAGE</code> Bundled skills (JAR resources) 1 (lowest) <code>GLOBAL</code> <code>~/.fraggle/skills/</code> 2 <code>PROJECT</code> <code>./config/skills/</code> (Fraggle root) 3 <code>EXPLICIT</code> <code>extra_paths</code> from config 4 (highest) <p>When two sources define a skill with the same name, the higher-precedence source wins \u2014 so you can ship a default skill in <code>~/.fraggle/skills/</code> and override it per project in <code>./config/skills/</code> without editing the original.</p> <p>Discovery rule: if a directory contains a <code>SKILL.md</code>, Fraggle treats it as a skill root and does not recurse into it. This means nested <code>SKILL.md</code> files under an existing skill are ignored \u2014 useful if a skill bundles reference material that itself contains markdown. For directories without a <code>SKILL.md</code>, Fraggle recurses looking for nested skill roots, so you can organise skills into categories:</p> <pre><code>config/skills/\n\u251c\u2500\u2500 review/\n\u2502   \u251c\u2500\u2500 code-review/SKILL.md\n\u2502   \u2514\u2500\u2500 docs-review/SKILL.md\n\u2514\u2500\u2500 writing/\n    \u2514\u2500\u2500 commit-message/SKILL.md\n</code></pre>"},{"location":"architecture/skills/#invocation","title":"Invocation","text":""},{"location":"architecture/skills/#automatic-model-initiated","title":"Automatic (model-initiated)","text":"<p>At startup, the catalog of visible skills is rendered into the system prompt as an XML block:</p> <pre><code>&lt;available_skills&gt;\n  &lt;skill&gt;\n    &lt;name&gt;code-review&lt;/name&gt;\n    &lt;description&gt;Review a pull request for correctness, style, and potential bugs.&lt;/description&gt;\n    &lt;location&gt;/home/you/.fraggle/skills/code-review/SKILL.md&lt;/location&gt;\n  &lt;/skill&gt;\n  ...\n&lt;/available_skills&gt;\n</code></pre> <p>The model is told to use the <code>read_file</code> tool to load a skill's body when the user's request matches one of the descriptions. After reading, it follows the instructions just like any other markdown guide. Relative paths inside a skill are resolved against the skill's directory.</p>"},{"location":"architecture/skills/#explicit-skillname","title":"Explicit (<code>/skill:name</code>)","text":"<p>In chat bridges, users can invoke a skill directly:</p> <pre><code>/skill:code-review please look at the changes in main.kt\n</code></pre> <p>Fraggle parses the command, reads the skill body, strips the frontmatter, and rewrites the user message as:</p> <pre><code>&lt;skill name=\"code-review\" location=\"/\u2026/code-review/SKILL.md\"&gt;\nReferences inside this skill are relative to /\u2026/code-review.\n\n# Code Review\n\nWhen the user asks for a code review:\n...\n&lt;/skill&gt;\n\nplease look at the changes in main.kt\n</code></pre> <p>That rewritten message then flows through the normal agent loop.</p> <p>Hidden skills (those with <code>disable-model-invocation: true</code>) are not in the catalog, so the model won't pick them up automatically \u2014 but they remain reachable via explicit <code>/skill:name</code>. Use this for skills you want to keep off the default surface (e.g. destructive operations) while still having them available on request.</p>"},{"location":"architecture/skills/#python-support","title":"Python support","text":"<p>Skills can include Python scripts with dependency management. When a skill directory contains a <code>requirements.txt</code>, Fraggle manages a per-skill Python virtual environment automatically.</p>"},{"location":"architecture/skills/#skill-layout-with-python","title":"Skill layout with Python","text":"<pre><code>my-api-skill/\n\u251c\u2500\u2500 SKILL.md\n\u251c\u2500\u2500 requirements.txt       # Python dependencies\n\u2514\u2500\u2500 scripts/\n    \u251c\u2500\u2500 config.py           # Shared credential/config helpers\n    \u251c\u2500\u2500 get_data.py         # Individual script per action\n    \u2514\u2500\u2500 submit_order.py\n</code></pre>"},{"location":"architecture/skills/#how-it-works","title":"How it works","text":"<ol> <li>On <code>fraggle skills add</code>: if <code>requirements.txt</code> is present, Fraggle automatically creates a venv at <code>~/.fraggle/venvs/&lt;skill-name&gt;/</code> and runs <code>pip install -r requirements.txt</code>.</li> <li>When the agent runs a skill script: it passes <code>skill=\"&lt;name&gt;\"</code> to the <code>execute_command</code> tool. This activates the venv (prepends its <code>bin/</code> to <code>PATH</code>) and injects configured secrets as environment variables \u2014 all at the process level, never visible to the LLM.</li> <li>Manual setup: run <code>fraggle skills setup [name]</code> to create or <code>fraggle skills setup --rebuild [name]</code> to recreate venvs.</li> </ol>"},{"location":"architecture/skills/#writing-python-skill-scripts","title":"Writing Python skill scripts","text":"<p>Scripts should use <code>argparse</code> for CLI arguments and read credentials from environment variables (injected automatically by Fraggle):</p> <pre><code>import argparse\nimport os\n\ndef main(symbol):\n    api_key = os.environ.get(\"MY_API_KEY\")\n    if not api_key:\n        print(\"Error: MY_API_KEY not configured\")\n        return\n    # ... use the API ...\n    print(f\"Result for {symbol}: ...\")\n\nif __name__ == \"__main__\":\n    parser = argparse.ArgumentParser()\n    parser.add_argument(\"--symbol\", required=True)\n    args = parser.parse_args()\n    main(args.symbol)\n</code></pre> <p>The SKILL.md body should describe which scripts are available and what arguments they accept, so the agent knows how to invoke them.</p>"},{"location":"architecture/skills/#secrets","title":"Secrets","text":"<p>Skills declare required environment variables in the <code>env</code> frontmatter field:</p> <pre><code>---\nname: my-api-skill\ndescription: Interact with the My API service.\nenv: ['MY_API_KEY', 'MY_ACCOUNT_ID']\n---\n</code></pre>"},{"location":"architecture/skills/#storage","title":"Storage","text":"<p>Secrets are stored as individual files at <code>~/.fraggle/secrets/&lt;skill-name&gt;/&lt;VAR_NAME&gt;</code>. Directory permissions are <code>rwx------</code> (700) and file permissions are <code>rw-------</code> (600).</p>"},{"location":"architecture/skills/#managing-secrets","title":"Managing secrets","text":"<pre><code># Set a secret (prompts for value on stdin to avoid shell history)\nfraggle skills secrets set my-api-skill MY_API_KEY\n\n# Set with value directly (less secure \u2014 visible in shell history)\nfraggle skills secrets set my-api-skill MY_API_KEY --value \"sk-...\"\n\n# List configured secret names (values are never shown)\nfraggle skills secrets list my-api-skill\n\n# Check which required env vars are configured vs missing\nfraggle skills secrets check my-api-skill\n\n# Remove a secret\nfraggle skills secrets remove my-api-skill MY_API_KEY\n</code></pre>"},{"location":"architecture/skills/#security-model","title":"Security model","text":"<ul> <li>Secrets never pass through the LLM. The agent only knows the skill name; secret values are injected into the process environment at the tool execution layer.</li> <li>Secrets are not included in prompts. The system prompt shows which env vars are configured (<code>configured=\"true\"</code>) but never their values.</li> <li>Script output is visible to the LLM. Skill authors should avoid printing secret values in their scripts' stdout/stderr.</li> <li>Secrets survive skill removal unless <code>--purge</code> is passed to <code>fraggle skills remove</code>.</li> </ul>"},{"location":"architecture/skills/#configuration","title":"Configuration","text":"<p>All skill behaviour is controlled from <code>config/fraggle.yaml</code>:</p> <pre><code>fraggle:\n  skills:\n    enabled: true                        # Load and advertise skills\n    skills_dir: ./config/skills          # Project-scoped directory (PROJECT precedence)\n    global_dir: ~/.fraggle/skills        # Cross-project directory (null to disable)\n    extra_paths: []                      # Additional files or directories (EXPLICIT precedence)\n    enable_slash_commands: true          # Enable /skill:name in chat bridges\n</code></pre> <p>Setting <code>enabled: false</code> wires an empty registry \u2014 no catalog block is injected into the system prompt and <code>/skill:name</code> commands fall through as unknown. Setting <code>enable_slash_commands: false</code> keeps auto-invocation working but disables the explicit <code>/skill:name</code> path.</p>"},{"location":"architecture/skills/#authoring-tips","title":"Authoring tips","text":"<ul> <li>Write the description for the model, not for humans. The description is the only part the model sees until it chooses to read the body, and it is what triggers automatic invocation. Include what the skill does and when to use it \u2014 keywords that will appear in user requests.</li> <li>Keep SKILL.md skimmable. The model will read the whole file; short numbered steps work better than long prose.</li> <li>Use relative paths to bundled assets. Scripts or reference docs shipped alongside <code>SKILL.md</code> can be referenced with plain relative paths \u2014 the expander tells the model they resolve against the skill's directory.</li> <li>Test with <code>/skill:name</code> first. Explicit invocation forces the skill body into context without having to rely on the model's automatic match. Once the workflow itself works, tweak the description until automatic invocation kicks in reliably.</li> </ul>"},{"location":"architecture/skills/#cli-reference","title":"CLI reference","text":"<p>The <code>fraggle skills</code> subcommand family is a lightweight skill package manager modelled on vercel-labs/skills, scoped to Fraggle's own skill directories. It does not write to other agents' directories (<code>.claude/skills</code>, <code>.cursor/skills</code>, \u2026) \u2014 use the JS <code>npx skills</code> tool for cross-agent installs.</p> Command What it does <code>fraggle skills list</code> List installed skills from the configured sources, with source, description, and on-disk location. Shows <code>[python]</code> and <code>[env:N/M]</code> markers. Flags: <code>-g</code>/<code>-p</code> to filter, <code>--all</code> to include hidden skills. <code>fraggle skills find &lt;terms\u2026&gt;</code> Substring-match skills by name and description. Multiple terms are AND-matched, case-insensitive. <code>fraggle skills validate [path]</code> Validate SKILL.md files at a path (or the configured sources if omitted). Exits non-zero on any error; warnings don't fail. CI-friendly. <code>fraggle skills init &lt;name&gt; [-d \"\u2026\"]</code> Scaffold a new <code>SKILL.md</code> under the target directory with a templated body. <code>fraggle skills add &lt;source&gt;</code> Install skills from a local path, a GitHub repo, or a git URL. Auto-creates Python venvs and warns about missing secrets. <code>fraggle skills update [&lt;names\u2026&gt;]</code> Re-fetch and reinstall tracked skills. Omit <code>&lt;names&gt;</code> to update everything tracked in the target's manifest. Supports <code>--dry-run</code>. <code>fraggle skills remove &lt;name\u2026&gt;</code> Uninstall skills previously installed via <code>add</code>. Cleans up venvs. Pass <code>--purge</code> to also remove secrets. <code>fraggle skills setup [name]</code> Set up Python venvs for skills with <code>requirements.txt</code>. Use <code>--rebuild</code> to recreate from scratch. Omit name to set up all. <code>fraggle skills secrets set &lt;skill&gt; &lt;var&gt;</code> Set a secret value for a skill (prompts stdin; use <code>--value</code> for non-interactive). <code>fraggle skills secrets list &lt;skill&gt;</code> List configured secret names (values not shown). <code>fraggle skills secrets check &lt;skill&gt;</code> Show required vs configured env vars for a skill. <code>fraggle skills secrets remove &lt;skill&gt; &lt;var&gt;</code> Remove a secret."},{"location":"architecture/skills/#target-resolution","title":"Target resolution","text":"<p><code>init</code>, <code>add</code>, and <code>remove</code> all share the same target resolution:</p> <ol> <li><code>--path &lt;dir&gt;</code> \u2014 explicit override.</li> <li><code>--global</code> \u2014 <code>SkillsConfig.globalDir</code> (default <code>~/.fraggle/skills</code>).</li> <li><code>--project</code> \u2014 <code>SkillsConfig.skillsDir</code> (default <code>./config/skills</code>).</li> <li>Default: <code>--global</code> if configured, else <code>--project</code>.</li> </ol>"},{"location":"architecture/skills/#source-syntax-for-add","title":"Source syntax for <code>add</code>","text":"<p><code>fraggle skills add</code> accepts four source forms, evaluated in this order:</p> Form Example Notes Local path <code>./my-skill</code>, <code>/abs/path/to/skill</code>, <code>~/skills/code-review</code> A single <code>SKILL.md</code>, a skill directory, or a parent directory containing multiple skill dirs. Existing local paths always win over shorthand parsing. GitHub shorthand <code>vercel-labs/skills</code>, <code>acme/tools@main</code>, <code>acme/tools/skills/code-review@dev</code> Fetched as a zipball from <code>codeload.github.com</code>. <code>@ref</code> selects a branch, tag, or commit SHA (defaults to <code>HEAD</code>). A path after the repo slices to a subdirectory. The last <code>@</code> in the string is treated as the ref delimiter, so subpath + ref combinations work. github.com URL <code>https://github.com/acme/tools/tree/main/skills/foo</code>, <code>https://github.com/acme/tools/blob/main/SKILL.md</code> <code>tree/&lt;ref&gt;/&lt;path&gt;</code> and <code>blob/&lt;ref&gt;/&lt;path&gt;</code> forms are parsed into ref + subpath. Git URL <code>https://gitlab.com/foo/bar.git</code>, <code>git@github.com:foo/bar.git</code> Requires <code>git</code> on <code>PATH</code>. Shells out to <code>git clone --depth=1 [--branch &lt;ref&gt;] \u2026</code>. <p>On install, <code>add</code>:</p> <ul> <li>Downloads (or clones) into a temp directory and runs <code>SkillLoader</code> against it, applying all the normal validation rules (name/description, <code>.gitignore</code>, dot-dir skip).</li> <li>Copies each discovered skill's directory into the target (or symlinks it, for local sources, with <code>--symlink</code>).</li> <li>Refuses to overwrite an existing destination \u2014 pass <code>--force</code> to replace it.</li> <li>Records each installation in <code>&lt;target&gt;/.fraggle-skills.json</code>. The manifest is what <code>remove</code> consults, so skills you drop into the target manually are not <code>remove</code>-able via the CLI and will be left alone.</li> </ul>"},{"location":"architecture/skills/#examples","title":"Examples","text":"<pre><code># Scaffold a new skill locally\nfraggle skills init pr-review -d \"Review a pull request for correctness, style, and tests.\"\n\n# Install from GitHub shorthand\nfraggle skills add vercel-labs/skills\n\n# Install one skill from a subdirectory on a branch\nfraggle skills add acme/tools/skills/code-review@dev\n\n# Install from a local directory you're authoring, as a symlink\nfraggle skills add --symlink ~/code/my-skill\n\n# Install from a GitLab repo\nfraggle skills add https://gitlab.com/foo/bar.git\n\n# List everything loaded (including installed + manually-dropped)\nfraggle skills list\n\n# Search across loaded skills\nfraggle skills find commit message\n\n# Refresh every tracked skill from its original source\nfraggle skills update\n\n# Refresh specific skills, showing what would change without touching disk\nfraggle skills update commit-message --dry-run\n\n# Uninstall one or more skills\nfraggle skills remove pr-review code-review\n\n# Validate a directory before shipping (CI-friendly)\nfraggle skills validate ./skills\n</code></pre>"},{"location":"architecture/skills/#notes-on-update","title":"Notes on <code>update</code>","text":"<p><code>update</code> re-resolves each tracked skill by parsing its manifest <code>source</code> label back into the original spec, fetches a fresh copy, and reinstalls over the existing directory with <code>force=true</code>. Per-entry rules:</p> <ul> <li>Local sources installed by copy \u2014 re-copies from the current source, so authoring changes propagate.</li> <li>Local sources installed with <code>--symlink</code> \u2014 skipped with a <code>= nothing to do</code> message. The symlink already points at live content, so re-copying would defeat the point.</li> <li>GitHub and git URL sources \u2014 always re-fetched. GitHub sources with no <code>@ref</code> pull the default branch; pinning <code>@ref</code> to a tag/SHA makes update idempotent.</li> <li>Stale or unparseable manifest entries \u2014 fail with a clear message and bump the command's exit code to <code>1</code> so scripts notice.</li> </ul> <p><code>update</code> is non-atomic: if the fetch succeeds but the install step fails partway, the old destination may already have been deleted. For pinned (<code>@ref</code>) installs this is essentially never seen; for <code>HEAD</code>-tracking installs the risk is the same as any other in-place package manager upgrade. If you need atomicity, run against a temporary target (<code>--path /tmp/staging</code>) and swap directories yourself.</p>"},{"location":"architecture/skills/#exit-codes","title":"Exit codes","text":"Code Meaning <code>0</code> Success (including idempotent <code>add</code> reruns that skip only on collision). <code>1</code> Validation or install failure \u2014 e.g. source had no valid skills, remote fetch failed, partial <code>remove</code>. <code>2</code> Usage error \u2014 unparseable source, missing local path, bad <code>--path</code>."},{"location":"architecture/skills/#example","title":"Example","text":"<p>A copyable reference skill lives at <code>docs/examples/skills/commit-message/</code>. Copy the directory into <code>~/.fraggle/skills/</code> to install it globally, or into <code>./config/skills/</code> for a single project \u2014 or just run:</p> <pre><code>fraggle skills add docs/examples/skills/commit-message\n</code></pre>"},{"location":"architecture/tools/","title":"Tools","text":"<p>Tools are the capabilities available to the Fraggle agent. They enable the agent to interact with the file system, web, shell, and more. Tools extend <code>AgentToolDef&lt;Args&gt;</code> and are collected into a <code>FraggleToolRegistry</code>.</p>"},{"location":"architecture/tools/#built-in-tools","title":"Built-in Tools","text":"<p>Fraggle includes several tool groups out of the box.</p>"},{"location":"architecture/tools/#filesystem-tools","title":"Filesystem Tools","text":"<p>File operations within the sandbox:</p> Tool Description Parameters <code>read_file</code> Read file contents <code>path</code> (required), <code>max_lines</code> (default: 1000) <code>write_file</code> Write content to a file <code>path</code> (required), <code>content</code> (required) <code>append_file</code> Append content to a file <code>path</code> (required), <code>content</code> (required) <code>list_files</code> List directory contents <code>path</code> (required), <code>recursive</code> (default: false) <code>search_files</code> Search for files by pattern <code>path</code> (required), <code>pattern</code> (required, supports <code>*</code> and <code>?</code> wildcards) <code>file_exists</code> Check if a file or directory exists <code>path</code> (required) <code>delete_file</code> Delete a file <code>path</code> (required)"},{"location":"architecture/tools/#web-tools","title":"Web Tools","text":"<p>Web fetching and browser automation:</p> Tool Description Parameters <code>fetch_webpage</code> Fetch content from a webpage <code>url</code> (required) <code>fetch_api</code> Fetch data from an API endpoint <code>url</code> (required), <code>method</code> (default: GET) <code>screenshot_page</code> Take a screenshot of a webpage <code>url</code> (required), <code>full_page</code> (default: false), <code>caption</code> (optional) <p>Playwright Tools</p> <p>The <code>fetch_webpage</code> tool uses Playwright for JavaScript rendering when configured. The <code>screenshot_page</code> tool requires Playwright configuration.</p>"},{"location":"architecture/tools/#playwright-setup","title":"Playwright Setup","text":"<p>For JavaScript-heavy websites, configure Playwright:</p> <p>Option 1: Browserless (Docker) <pre><code>docker run -p 3000:3000 browserless/chrome\n</code></pre></p> <p>Option 2: Playwright Server <pre><code>npx playwright run-server --port 3000\n</code></pre></p> <p>Configuration: <pre><code>fraggle:\n  web:\n    playwright:\n      ws_endpoint: ws://localhost:3000\n      navigation_timeout: 30000\n      wait_after_load: 2000\n</code></pre></p>"},{"location":"architecture/tools/#shell-tools","title":"Shell Tools","text":"<p>Command execution in the sandbox:</p> Tool Description Parameters <code>execute_command</code> Execute a shell command <code>command</code> (required), <code>timeout_seconds</code> (default: 30) <p>Security</p> <p>Shell execution includes basic safeguards against dangerous commands (like <code>rm -rf /</code>). Use supervision for interactive approval of tool calls.</p>"},{"location":"architecture/tools/#scheduling-tools","title":"Scheduling Tools","text":"<p>Task scheduling for deferred operations:</p> Tool Description Parameters <code>schedule_task</code> Schedule a task for later <code>name</code>, <code>action</code>, <code>delay_seconds</code>, <code>repeat_interval_seconds</code> (optional) <code>list_tasks</code> List all scheduled tasks (none) <code>get_task</code> Get task details <code>task_id</code> <code>cancel_task</code> Cancel a scheduled task <code>task_id</code>"},{"location":"architecture/tools/#time-tools","title":"Time Tools","text":"<p>Time and timezone utilities:</p> Tool Description Parameters <code>get_current_time</code> Get current date/time in any timezone <code>timezone</code> (optional, IANA ID) <p>The agent also receives the host server's current ISO 8601 timestamp in every system prompt, so it always knows the current date and time without needing a tool call. The <code>get_current_time</code> tool is used when the agent needs to convert to a specific timezone (e.g., answering \"What time is it in Tokyo?\").</p> <p>The agent can determine a user's local time by:</p> <ul> <li>Checking the user's timezone from memory (auto-extracted from conversations)</li> <li>Using the <code>Timezone</code> field in the user profile (<code>USER.md</code>)</li> <li>Asking the user for their location</li> </ul>"},{"location":"architecture/tools/#defining-custom-tools","title":"Defining Custom Tools","text":"<p>Tools extend <code>AgentToolDef&lt;Args&gt;</code> with a <code>@Serializable</code> data class for parameters:</p> <pre><code>class WeatherTool(private val apiClient: HttpClient) : AgentToolDef&lt;WeatherTool.Args&gt;(\n    name = \"get_weather\",\n    description = \"Get current weather for a location\",\n    argsSerializer = Args.serializer(),\n) {\n    @Serializable\n    data class Args(\n        @LLMDescription(\"City name or coordinates\")\n        val location: String,\n    )\n\n    override suspend fun execute(args: Args): String {\n        // Call weather API...\n        return \"Weather in ${args.location}: Sunny, 72F\"\n    }\n}\n</code></pre> <p>Key points:</p> <ul> <li><code>@Serializable</code> on the <code>Args</code> data class enables automatic JSON schema generation for the LLM</li> <li><code>@LLMDescription</code> on each field provides the parameter description shown to the LLM</li> <li>Default values on fields make parameters optional (e.g., <code>val max_lines: Int = 1000</code>)</li> <li><code>execute</code> returns a <code>String</code> result that the LLM uses for further reasoning</li> </ul>"},{"location":"architecture/tools/#tool-registry","title":"Tool Registry","text":"<p>Tools are collected into a <code>FraggleToolRegistry</code>:</p> <pre><code>val registry = FraggleToolRegistry(\n    tools = listOf(MyTool().managed(supervisor, remoteClient)),\n)\n</code></pre> <p>The built-in <code>DefaultTools.createToolRegistry()</code> registers all standard tools:</p> <pre><code>val registry = DefaultTools.createToolRegistry(\n    toolExecutor = toolExecutor,\n    httpClient = httpClient,\n    taskScheduler = taskScheduler,\n    supervisor = supervisor,\n    remoteClient = remoteClient,            // null for local-only\n    playwrightFetcher = playwrightFetcher,  // optional\n)\n</code></pre>"},{"location":"architecture/tools/#supervision-and-remote-execution","title":"Supervision and Remote Execution","text":"<p>Every tool that performs I/O is wrapped in a <code>ManagedTool</code> before being added to the registry. <code>ManagedTool</code> adds two layers around the underlying tool:</p> <ol> <li> <p>Supervision \u2014 Before executing, the tool checks with a <code>ToolSupervisor</code>. In <code>none</code> mode (<code>NoOpToolSupervisor</code>), all calls are auto-approved. In <code>supervised</code> mode (<code>InteractiveToolSupervisor</code>), the tool call is evaluated against policy-based <code>tool_policies</code> rules. Each rule can specify <code>allow</code>, <code>deny</code>, or <code>ask</code> policies, with optional argument pattern constraints (e.g., deny <code>write_file</code> for paths under <code>/etc/**</code>). How patterns are matched depends on the <code>@ToolArg</code> annotation on the tool's Args property: <code>PATH</code> arguments are normalized before glob matching to prevent traversal attacks, <code>SHELL_COMMAND</code> arguments are parsed and each command is matched against patterns (simple flag-aware <code>value</code> patterns or structured <code>commands</code> with flag allowlists/denylists and typed positional arg matching), and unannotated arguments use plain glob matching. If no rule matches, a permission request is sent to the user.</p> </li> <li> <p>Remote forwarding \u2014 If a <code>RemoteToolClient</code> is configured (<code>type: remote</code>), the call is forwarded over HTTP to a worker process instead of executing locally.</p> </li> </ol> <pre><code>ManagedTool.execute(args)\n  \u2502\n  \u251c\u2500 Supervisor: evaluate policy rules (first match wins)\n  \u2502   \u251c\u2500 policy: allow \u2192 Approved\n  \u2502   \u251c\u2500 policy: deny  \u2192 Denied (no user prompt)\n  \u2502   \u251c\u2500 policy: ask   \u2192 Delegate to user\n  \u2502   \u251c\u2500 no match      \u2192 Delegate to user\n  \u2502   \u2502   \u251c\u2500 CLI prompt (fraggle chat) \u2192 Approved / Denied\n  \u2502   \u2502   \u2514\u2500 WebSocket event (fraggle run) \u2192 Approved / Denied\n  \u2502\n  \u251c\u2500 Remote client present? \u2192 Forward to worker via HTTP\n  \u2514\u2500 Otherwise \u2192 Execute delegate tool locally\n</code></pre> <p>Scheduling tools (<code>schedule_task</code>, <code>list_tasks</code>, <code>get_task</code>, <code>cancel_task</code>) are not wrapped because they don't perform external I/O.</p> <p>See Executor configuration for the YAML options.</p>"},{"location":"architecture/tools/#execution-context","title":"Execution Context","text":"<p>Tools can access per-request context (chat ID, user ID) via <code>ToolExecutionContext</code>:</p> <pre><code>override suspend fun execute(args: Args): String {\n    val context = ToolExecutionContext.current()\n    val chatId = context?.chatId\n    // Use context for chat-specific behavior\n    return \"Response for chat $chatId\"\n}\n</code></pre> <p>Tools that produce binary output (like <code>screenshot_page</code>) add attachments to the context, which the agent sends alongside the text response.</p>"},{"location":"development/building/","title":"Building","text":"<p>This guide covers building Fraggle from source for development purposes.</p> <p>For Users</p> <p>If you just want to run Fraggle, download the latest release instead of building from source. See the Getting Started guide.</p>"},{"location":"development/building/#prerequisites","title":"Prerequisites","text":"<ul> <li>JDK 21+ - Required for compilation and runtime</li> <li>Gradle - Wrapper included (no separate installation needed)</li> </ul>"},{"location":"development/building/#build-commands","title":"Build Commands","text":""},{"location":"development/building/#build-all-modules","title":"Build All Modules","text":"<pre><code>./gradlew build\n</code></pre> <p>This compiles all modules and runs tests.</p>"},{"location":"development/building/#build-without-tests","title":"Build Without Tests","text":"<pre><code>./gradlew build -x test\n</code></pre>"},{"location":"development/building/#build-specific-module","title":"Build Specific Module","text":"<pre><code># Core framework\n./gradlew :fraggle-assistant:build\n\n# Signal integration\n./gradlew :fraggle-signal:build\n\n# Built-in tools\n./gradlew :fraggle-tools:build\n\n# CLI application\n./gradlew :fraggle-cli:build\n</code></pre>"},{"location":"development/building/#distribution","title":"Distribution","text":""},{"location":"development/building/#create-runnable-distribution","title":"Create Runnable Distribution","text":"<pre><code>./gradlew :fraggle-cli:installDist\n</code></pre> <p>Creates a runnable distribution at <code>fraggle-cli/build/install/fraggle-cli/</code>.</p> <p>Run with: <pre><code>./fraggle-cli/build/install/fraggle-cli/bin/fraggle run\n</code></pre></p>"},{"location":"development/building/#create-distribution-archives","title":"Create Distribution Archives","text":"<pre><code>./gradlew :fraggle-cli:distZip\n./gradlew :fraggle-cli:distTar\n</code></pre> <p>Creates archives at <code>fraggle-cli/build/distributions/</code>.</p>"},{"location":"development/building/#build-fat-jar","title":"Build Fat JAR","text":"<pre><code>./gradlew :fraggle-cli:shadowJar\n</code></pre> <p>Creates an executable JAR with all dependencies at <code>fraggle-cli/build/libs/fraggle.jar</code>.</p> <p>Run with: <pre><code>java -jar fraggle-cli/build/libs/fraggle.jar run\n</code></pre></p>"},{"location":"development/building/#development-mode","title":"Development Mode","text":"<p>When working on Fraggle itself, use the Gradle run task which configures <code>FRAGGLE_ROOT</code> for development:</p> <pre><code># Clone the repo\ngit clone https://github.com/DrewCarlson/Fraggle.git\ncd Fraggle\n\n# Set up dev config\nmkdir -p runtime-dev/config\ncp config/fraggle.yaml runtime-dev/config/fraggle.yaml\n\n# Run in dev mode (sets FRAGGLE_ROOT=runtime-dev/)\n./gradlew :fraggle-cli:run --args=\"chat\"\n</code></pre>"},{"location":"development/building/#dashboard-build","title":"Dashboard Build","text":"<p>The web dashboard uses Kotlin/JS with Compose for Web:</p> <pre><code># Development build\n./gradlew :fraggle-dashboard:jsBrowserDevelopmentDist\n\n# Production build\n./gradlew :fraggle-dashboard:jsBrowserProductionDist\n</code></pre> <p>Output is at <code>fraggle-dashboard/build/vite/js/productionExecutable/</code>.</p>"},{"location":"development/building/#clean-build","title":"Clean Build","text":"<pre><code># Clean all build outputs\n./gradlew clean\n\n# Clean and rebuild\n./gradlew clean build\n</code></pre>"},{"location":"development/building/#dependency-updates","title":"Dependency Updates","text":"<p>Check for dependency updates:</p> <pre><code>./gradlew dependencyUpdates\n</code></pre>"},{"location":"development/building/#ide-setup","title":"IDE Setup","text":""},{"location":"development/building/#intellij-idea","title":"IntelliJ IDEA","text":"<ol> <li>Open the project directory in IntelliJ</li> <li>IntelliJ should automatically detect the Gradle project</li> <li>Wait for the Gradle sync to complete</li> <li>Use the Gradle tool window for build tasks</li> </ol>"},{"location":"development/building/#vs-code","title":"VS Code","text":"<ol> <li>Install the Kotlin and Gradle extensions</li> <li>Open the project directory</li> <li>Use the Gradle extension for build tasks</li> </ol>"},{"location":"development/building/#troubleshooting","title":"Troubleshooting","text":""},{"location":"development/building/#unsupported-class-file-major-version","title":"\"Unsupported class file major version\"","text":"<p>You're using a JDK version lower than 21. Check with: <pre><code>java -version\n</code></pre></p>"},{"location":"development/building/#out-of-memory","title":"Out of Memory","text":"<p>Increase Gradle's heap size in <code>gradle.properties</code>: <pre><code>org.gradle.jvmargs=-Xmx4g\n</code></pre></p>"},{"location":"development/building/#gradle-daemon-issues","title":"Gradle Daemon Issues","text":"<p>Kill existing daemons: <pre><code>./gradlew --stop\n</code></pre></p>"},{"location":"development/contributing/","title":"Contributing","text":"<p>Thank you for your interest in contributing to Fraggle.</p>"},{"location":"development/contributing/#development-setup","title":"Development Setup","text":"<ol> <li> <p>Clone the repository <pre><code>git clone https://github.com/DrewCarlson/Fraggle.git\ncd Fraggle\n</code></pre></p> </li> <li> <p>Verify the build works <pre><code>./gradlew build\n</code></pre></p> </li> <li> <p>Set up your IDE</p> </li> <li>IntelliJ IDEA is recommended</li> <li> <p>Open the project directory and let it sync</p> </li> <li> <p>Create a development config <pre><code>mkdir -p runtime-dev/config\ncp config/fraggle.yaml runtime-dev/config/fraggle.yaml\n</code></pre></p> </li> <li> <p>Run in development mode <pre><code>./gradlew :fraggle-cli:run --args=\"chat\"\n</code></pre></p> </li> </ol>"},{"location":"development/contributing/#code-style","title":"Code Style","text":"<p>The project follows standard Kotlin coding conventions:</p> <ul> <li>Use 4 spaces for indentation</li> <li>Follow Kotlin coding conventions</li> <li>Keep functions focused and small</li> <li>Write descriptive names for classes, functions, and variables</li> <li>Document public APIs with KDoc</li> </ul>"},{"location":"development/contributing/#making-changes","title":"Making Changes","text":""},{"location":"development/contributing/#1-create-a-branch","title":"1. Create a Branch","text":"<pre><code>git checkout -b feature/my-feature\n# or\ngit checkout -b fix/bug-description\n</code></pre>"},{"location":"development/contributing/#2-make-your-changes","title":"2. Make Your Changes","text":"<ul> <li>Write code</li> <li>Add/update tests</li> <li>Update documentation if needed</li> </ul>"},{"location":"development/contributing/#3-run-tests","title":"3. Run Tests","text":"<pre><code>./gradlew test\n</code></pre>"},{"location":"development/contributing/#4-commit","title":"4. Commit","text":"<p>Write clear commit messages:</p> <pre><code>Add weather tool for fetching forecasts\n\n- Implement WeatherTool with location parameter\n- Add tests for various weather conditions\n- Document tool in tools.md\n</code></pre>"},{"location":"development/contributing/#5-push-and-create-pr","title":"5. Push and Create PR","text":"<pre><code>git push origin feature/my-feature\n</code></pre> <p>Then create a pull request on GitHub.</p>"},{"location":"development/contributing/#pull-request-guidelines","title":"Pull Request Guidelines","text":"<ul> <li>Provide a clear description of the changes</li> <li>Reference any related issues</li> <li>Ensure tests pass</li> <li>Keep PRs focused - one feature/fix per PR</li> <li>Be responsive to feedback</li> </ul>"},{"location":"development/contributing/#areas-to-contribute","title":"Areas to Contribute","text":""},{"location":"development/contributing/#good-first-issues","title":"Good First Issues","text":"<p>Look for issues labeled <code>good first issue</code> on GitHub.</p>"},{"location":"development/contributing/#tools","title":"Tools","text":"<p>Add new tools for:</p> <ul> <li>External APIs (weather, news, etc.)</li> <li>System operations</li> <li>Integrations with other services</li> </ul>"},{"location":"development/contributing/#documentation","title":"Documentation","text":"<ul> <li>Improve existing docs</li> <li>Add examples</li> <li>Fix typos</li> </ul>"},{"location":"development/contributing/#testing","title":"Testing","text":"<ul> <li>Increase test coverage</li> <li>Add integration tests</li> <li>Improve test utilities</li> </ul>"},{"location":"development/contributing/#performance","title":"Performance","text":"<ul> <li>Profile and optimize hot paths</li> <li>Reduce memory usage</li> <li>Improve startup time</li> </ul>"},{"location":"development/contributing/#architecture-notes","title":"Architecture Notes","text":""},{"location":"development/contributing/#adding-a-new-tool","title":"Adding a New Tool","text":"<ol> <li>Create a class extending <code>SimpleTool&lt;Args&gt;</code> in <code>fraggle-tools/src/main/kotlin/</code></li> <li>Define a <code>@Serializable</code> <code>Args</code> data class with <code>@LLMDescription</code> annotations</li> <li>Register in <code>DefaultTools.createToolRegistry()</code></li> <li>Add tests</li> <li>Document in <code>docs/architecture/tools.md</code></li> </ol>"},{"location":"development/contributing/#adding-a-new-bridge","title":"Adding a New Bridge","text":"<ol> <li>Implement bridge interface</li> <li>Add configuration in <code>ConfigModels.kt</code></li> <li>Wire up in service orchestrator</li> <li>Add documentation</li> </ol>"},{"location":"development/contributing/#questions","title":"Questions?","text":"<ul> <li>Open an issue for questions or discussion</li> <li>Check existing issues before opening new ones</li> <li>Be patient and respectful in discussions</li> </ul>"},{"location":"development/contributing/#license","title":"License","text":"<p>By contributing, you agree that your contributions will be licensed under the MIT License.</p>"},{"location":"development/testing/","title":"Testing","text":"<p>This guide covers running and writing tests for Fraggle.</p>"},{"location":"development/testing/#running-tests","title":"Running Tests","text":""},{"location":"development/testing/#all-tests","title":"All Tests","text":"<pre><code>./gradlew test\n</code></pre>"},{"location":"development/testing/#specific-module","title":"Specific Module","text":"<pre><code>./gradlew :fraggle-assistant:test\n./gradlew :fraggle-tools:test\n./gradlew :fraggle-signal:test\n</code></pre>"},{"location":"development/testing/#specific-test-class","title":"Specific Test Class","text":"<pre><code>./gradlew :fraggle-assistant:test --tests=\"*FileMemoryStoreTest\"\n./gradlew :fraggle-tools:test --tests=\"*ToolsTest\"\n</code></pre>"},{"location":"development/testing/#specific-test-method","title":"Specific Test Method","text":"<pre><code>./gradlew :fraggle-assistant:test --tests=\"*FileMemoryStoreTest.UpdateFactTests*\"\n</code></pre>"},{"location":"development/testing/#with-detailed-output","title":"With Detailed Output","text":"<pre><code>./gradlew test --info\n</code></pre>"},{"location":"development/testing/#test-framework","title":"Test Framework","text":"<p>Tests use:</p> <ul> <li>JUnit 5 - Test framework</li> <li>MockK - Kotlin-friendly mocking</li> <li>kotlinx-coroutines-test - Coroutine testing utilities</li> <li>Kotest Assertions - Fluent assertions (optional)</li> </ul>"},{"location":"development/testing/#writing-tests","title":"Writing Tests","text":""},{"location":"development/testing/#basic-test","title":"Basic Test","text":"<pre><code>class MyTest {\n    @Test\n    fun `test description`() {\n        val result = someFunction()\n        assertEquals(expected, result)\n    }\n}\n</code></pre>"},{"location":"development/testing/#coroutine-test","title":"Coroutine Test","text":"<pre><code>class CoroutineTest {\n    @Test\n    fun `test async operation`() = runTest {\n        val result = suspendingFunction()\n        assertEquals(expected, result)\n    }\n}\n</code></pre>"},{"location":"development/testing/#mocking-with-mockk","title":"Mocking with MockK","text":"<pre><code>class MockTest {\n    private val mockSandbox = mockk&lt;Sandbox&gt;()\n\n    @Test\n    fun `test with mock`() = runTest {\n        every { mockSandbox.readFile(any(), any()) } returns\n            SandboxResult.Success(\"file contents\")\n\n        val tool = ReadFileTool(mockSandbox)\n        val result = tool.execute(ReadFileTool.Args(path = \"test.txt\"))\n\n        assertEquals(\"file contents\", result)\n        verify { mockSandbox.readFile(\"test.txt\", 1000) }\n    }\n}\n</code></pre>"},{"location":"development/testing/#testing-tools","title":"Testing Tools","text":"<p>Tools extend <code>AgentToolDef</code> and can be tested directly by calling <code>execute</code>:</p> <pre><code>class ToolTest {\n    private val sandbox = mockk&lt;Sandbox&gt;()\n\n    @Test\n    fun `test read_file tool`() = runTest {\n        every { sandbox.readFile(any(), any()) } returns\n            SandboxResult.Success(\"file contents\")\n\n        val tool = ReadFileTool(sandbox)\n        val result = tool.execute(ReadFileTool.Args(path = \"test.txt\"))\n\n        assertEquals(\"file contents\", result)\n    }\n}\n</code></pre>"},{"location":"development/testing/#test-categories","title":"Test Categories","text":""},{"location":"development/testing/#unit-tests","title":"Unit Tests","text":"<p>Test individual components in isolation:</p> <pre><code>// Located in src/test/kotlin/\nclass MemoryStoreTest {\n    @Test\n    fun `store should save and load facts`() = runTest {\n        val store = FileMemoryStore(tempDir)\n        store.append(MemoryScope.Global, Fact(\"Test fact\"))\n\n        val memory = store.load(MemoryScope.Global)\n        assertEquals(1, memory.facts.size)\n        assertEquals(\"Test fact\", memory.facts[0].content)\n    }\n}\n</code></pre>"},{"location":"development/testing/#integration-tests","title":"Integration Tests","text":"<p>Test component interactions:</p> <pre><code>class ToolIntegrationTest {\n    @Test\n    fun `tool should execute within sandbox`() = runTest {\n        val sandbox = PermissiveSandbox(tempDir)\n        tempDir.resolve(\"test.txt\").writeText(\"hello world\")\n\n        val tool = ReadFileTool(sandbox)\n        val result = tool.execute(ReadFileTool.Args(path = \"test.txt\"))\n\n        assertTrue(result.contains(\"hello world\"))\n    }\n}\n</code></pre>"},{"location":"development/testing/#test-fixtures","title":"Test Fixtures","text":""},{"location":"development/testing/#sample-configurations","title":"Sample Configurations","text":"<pre><code>object TestFixtures {\n    val sampleConfig = FraggleConfig(\n        fraggle = FraggleSettings(\n            provider = ProviderConfig(\n                type = ProviderType.LMSTUDIO,\n                url = \"http://localhost:1234/v1\"\n            )\n        )\n    )\n}\n</code></pre>"},{"location":"development/testing/#in-memory-implementations","title":"In-Memory Implementations","text":"<pre><code>// InMemoryStore for testing memory without filesystem\nval store = InMemoryStore()\nstore.append(MemoryScope.Global, Fact(\"Test fact\"))\n</code></pre>"},{"location":"development/testing/#coverage","title":"Coverage","text":"<p>Generate coverage reports:</p> <pre><code>./gradlew test jacocoTestReport\n</code></pre> <p>Reports are at <code>build/reports/jacoco/</code>.</p>"},{"location":"development/testing/#continuous-integration","title":"Continuous Integration","text":"<p>Tests run automatically on:</p> <ul> <li>Pull requests</li> <li>Pushes to main branch</li> </ul> <p>Check <code>.github/workflows/</code> for CI configuration.</p>"},{"location":"development/testing/#best-practices","title":"Best Practices","text":"<ol> <li>Test behavior, not implementation - Focus on what the code does, not how</li> <li>Use descriptive test names - Use backticks for readable names: <code>`should return error when file not found`</code></li> <li>Arrange-Act-Assert - Structure tests clearly</li> <li>One assertion per test - When practical, test one thing at a time</li> <li>Mock external dependencies - Don't make real network calls in unit tests</li> </ol>"},{"location":"examples/skills/commit-message/SKILL/","title":"Conventional Commit Message","text":"<p>When the user asks you to write a commit message:</p> <ol> <li>Run <code>git diff --cached</code> to see the staged changes. If nothing is staged, run <code>git diff</code> and note that the user has unstaged work.</li> <li>Identify the dominant change kind: <code>feat</code>, <code>fix</code>, <code>refactor</code>, <code>docs</code>, <code>test</code>, <code>chore</code>, <code>build</code>, <code>ci</code>, <code>perf</code>, <code>style</code>.</li> <li>Pick a concise scope (the module, package, or feature area the change touches). Skip the scope if the change is repository-wide.</li> <li>Write the subject line as <code>type(scope): summary</code> \u2014 imperative mood, \u226472 characters, no trailing period.</li> <li>If the change is non-trivial, add a blank line and a short body (1\u20133 sentences) explaining the why, not the what.</li> <li>If the change introduces a breaking change, add a <code>BREAKING CHANGE:</code> footer describing the migration.</li> </ol> <p>Return only the commit message text \u2014 no preamble, no markdown fences.</p>"},{"location":"examples/skills/commit-message/SKILL/#example","title":"Example","text":"<pre><code>feat(auth): add rotating refresh tokens\n\nRefresh tokens are now rotated on every use and old tokens\nare revoked, closing the replay window exploited in issue #412.\n</code></pre>"},{"location":"examples/skills/weather-api/SKILL/","title":"Weather API","text":"<p>Provides current weather data and forecasts via OpenWeatherMap.</p>"},{"location":"examples/skills/weather-api/SKILL/#setup","title":"Setup","text":"<p>This skill requires a free API key from OpenWeatherMap.</p> <pre><code>fraggle skills secrets set weather-api WEATHER_API_KEY\n</code></pre>"},{"location":"examples/skills/weather-api/SKILL/#available-scripts","title":"Available Scripts","text":""},{"location":"examples/skills/weather-api/SKILL/#get-current-weather","title":"Get Current Weather","text":"<pre><code>python scripts/get_weather.py --city \"London\"\n</code></pre> <p>Returns current temperature, conditions, humidity, and wind speed.</p>"},{"location":"examples/skills/weather-api/SKILL/#get-forecast","title":"Get Forecast","text":"<pre><code>python scripts/get_forecast.py --city \"London\" --days 3\n</code></pre> <p>Returns a multi-day forecast with high/low temperatures and conditions.</p>"},{"location":"examples/skills/weather-api/SKILL/#usage-notes","title":"Usage Notes","text":"<ul> <li>City names are case-insensitive.</li> <li>Use <code>execute_command</code> with <code>skill=\"weather-api\"</code> to run scripts. The API key is injected automatically.</li> <li>All temperatures are returned in Celsius.</li> </ul>"},{"location":"installation/configuration/","title":"Configuration","text":"<p>Fraggle uses a YAML configuration file located at <code>$FRAGGLE_ROOT/config/fraggle.yaml</code>.</p>"},{"location":"installation/configuration/#configuration-wizard","title":"Configuration Wizard","text":"<p>The easiest way to configure Fraggle is with the interactive wizard:</p> <pre><code>./bin/fraggle configure\n</code></pre> <p>The wizard offers two modes:</p> <ul> <li>Quick setup - Configure essential settings: LLM provider, chat bridge, and API server</li> <li>Full setup - Walk through all configuration sections</li> </ul> <p>If a configuration file already exists, the wizard will use your current values as defaults.</p>"},{"location":"installation/configuration/#environment-variables","title":"Environment Variables","text":"Variable Description Default <code>FRAGGLE_ROOT</code> Root directory for all runtime files (config, data, logs) Current directory"},{"location":"installation/configuration/#full-configuration-reference","title":"Full Configuration Reference","text":"<pre><code>fraggle:\n  # LLM Provider settings\n  provider:\n    type: lmstudio                    # lmstudio, openai, anthropic\n    url: http://localhost:1234/v1     # API endpoint\n    model: \"\"                         # Model name (optional for LM Studio)\n    api_key: \"\"                       # Required for OpenAI/Anthropic\n\n  # Chat bridge configurations\n  bridges:\n    signal:\n      enabled: true\n      phone: \"+1234567890\"            # Your Signal phone number\n      config_dir: ./config/apps/signal\n      trigger: \"@fraggle\"             # Prefix to trigger bot in groups\n      signal_cli_path: null           # Path to signal-cli (null = auto-detect)\n      auto_install: true              # Auto-download signal-cli if not found\n      signal_cli_version: \"0.14.2\"   # Version to auto-install\n      respond_to_direct_messages: true\n      show_typing_indicator: true\n\n  # Prompt file configuration\n  prompts:\n    prompts_dir: ./config/prompts     # Directory for prompt files\n    max_file_chars: 20000             # Max chars per prompt file\n    auto_create_missing: true         # Auto-create from templates\n\n  # Memory storage settings\n  memory:\n    base_dir: ./data/memory\n\n  # Tool execution settings\n  executor:\n    type: local                       # local or remote\n    work_dir: ./data/workspace\n    remote_url: \"\"                    # Worker URL (required when type: remote)\n    supervision: none                 # none or supervised\n    tool_policies: []                  # Policy-based rules for tool approval (allow, deny, ask)\n\n  # Agent behavior settings\n  agent:\n    temperature: 0.7                  # LLM sampling temperature (0.0-2.0)\n    max_tokens: 4096                  # Max response tokens\n    max_iterations: 10                # Max tool-use iterations\n    max_history_messages: 20          # Messages to include in context\n\n  # Registered chats with custom settings\n  chats:\n    registered: []\n    # - id: \"group-abc123\"\n    #   name: \"Dev Team\"\n    #   trigger_override: \"@bot\"\n    #   enabled: true\n\n  # Web browsing configuration\n  web:\n    playwright:                       # Optional Playwright config\n      ws_endpoint: ws://localhost:3000\n      navigation_timeout: 30000\n      wait_after_load: 2000\n      viewport_width: 1280\n      viewport_height: 720\n      user_agent: null                # null = browser default\n\n  # Agent skills (see architecture/skills.md)\n  skills:\n    enabled: true\n    skills_dir: ./config/skills       # Project-scoped skills directory\n    global_dir: ~/.fraggle/skills     # Cross-project directory (null to disable)\n    extra_paths: []                   # Additional files or directories\n    enable_slash_commands: true       # /skill:name invocation in chat bridges\n\n  # Tracing\n  tracing:\n    level: off                        # off, metadata, full\n\n  # REST API server\n  api:\n    enabled: false\n    host: \"0.0.0.0\"\n    port: 9191\n    cors:\n      enabled: true\n      allowed_origins: []\n\n  # Web dashboard\n  dashboard:\n    enabled: false\n    static_path: null                 # null = use embedded assets\n</code></pre>"},{"location":"installation/configuration/#configuration-sections","title":"Configuration Sections","text":""},{"location":"installation/configuration/#provider","title":"Provider","text":"<p>Configures the LLM provider connection.</p> Option Description Default <code>type</code> Provider type: <code>lmstudio</code>, <code>openai</code>, <code>anthropic</code> <code>lmstudio</code> <code>url</code> API endpoint URL <code>http://localhost:1234/v1</code> <code>model</code> Model identifier (optional for LM Studio) <code>\"\"</code> <code>api_key</code> API key for authentication <code>null</code>"},{"location":"installation/configuration/#bridges","title":"Bridges","text":"<p>Chat platform integrations. Currently supports Signal.</p> <p>Signal Bridge - Fraggle can automatically download and install signal-cli if it's not found in your system PATH. This is enabled by default (<code>auto_install: true</code>). On Linux, the native binary is downloaded for better performance; on other platforms, the Java-based version is used.</p> Option Description Default <code>phone</code> Signal phone number (with country code) Required <code>enabled</code> Whether bridge is active <code>true</code> if phone is set <code>config_dir</code> Directory for Signal configuration <code>~/.config/fraggle/signal</code> <code>trigger</code> Prefix to trigger bot in group chats <code>@fraggle</code> <code>signal_cli_path</code> Path to signal-cli (null = auto-detect) <code>null</code> <code>auto_install</code> Auto-download signal-cli if not found <code>true</code> <code>signal_cli_version</code> Version of signal-cli to auto-install <code>0.14.2</code> <code>respond_to_direct_messages</code> Respond to DMs without trigger <code>true</code> <code>show_typing_indicator</code> Show typing indicator while processing <code>true</code> <p>See Signal Setup for detailed Signal configuration and registration.</p>"},{"location":"installation/configuration/#prompts","title":"Prompts","text":"<p>The prompt system uses markdown files for customization:</p> <ul> <li><code>SYSTEM.md</code> - Core system prompt and instructions</li> <li><code>IDENTITY.md</code> - Agent personality and identity</li> <li><code>USER.md</code> - User-specific instructions and preferences</li> </ul> <p>Files are loaded from <code>prompts_dir</code> and concatenated to form the full system prompt.</p>"},{"location":"installation/configuration/#memory","title":"Memory","text":"<p>Memory is stored as human-readable markdown files in hierarchical scopes:</p> <ul> <li>Global - Shared across all conversations (<code>memory/global/</code>)</li> <li>Chat - Per-conversation memory (<code>memory/chats/{chat_id}/</code>)</li> <li>User - Per-user memory (<code>memory/users/{user_id}/</code>)</li> </ul>"},{"location":"installation/configuration/#executor","title":"Executor","text":"<p>Controls how tools execute file and shell operations. The YAML key is <code>sandbox</code> for backward compatibility.</p> Option Description Default <code>type</code> Execution mode: <code>local</code> or <code>remote</code> <code>local</code> <code>work_dir</code> Working directory for tool operations <code>./data/workspace</code> <code>remote_url</code> URL of the remote worker (required when <code>type: remote</code>) <code>\"\"</code> <code>supervision</code> Permission mode: <code>none</code> or <code>supervised</code> <code>none</code> <code>tool_policies</code> Policy-based rules for tool approval (allow, deny, ask with arg matchers and shell-aware command matching) <code>[]</code>"},{"location":"installation/configuration/#execution-modes","title":"Execution Modes","text":"<p>Local (<code>type: local</code>) \u2014 Tools run directly in the Fraggle process. This is the default and simplest mode.</p> <p>Remote (<code>type: remote</code>) \u2014 Tool calls are forwarded over HTTP to a separate worker process. This lets you isolate tool execution on another machine or in a container. You must set <code>remote_url</code> to the worker's address and start a worker with <code>fraggle worker</code>.</p> <pre><code>executor:\n  type: remote\n  remote_url: http://worker-host:9292\n</code></pre>"},{"location":"installation/configuration/#supervision","title":"Supervision","text":"<p>When <code>supervision: supervised</code> is set, every tool call requires explicit approval before it runs. How approval works depends on how Fraggle is started:</p> <ul> <li><code>fraggle chat</code> \u2014 Prompts on the terminal (stdin). You have 60 seconds to respond <code>y</code> or <code>n</code>.</li> <li><code>fraggle run</code> \u2014 Emits a permission request event over the WebSocket API. The dashboard or any connected client can approve or deny the request within 120 seconds.</li> </ul> <p>Rules in <code>tool_policies</code> are evaluated top-to-bottom, first match wins. Each rule specifies a <code>tool</code> name and an optional <code>policy</code> (<code>allow</code>, <code>deny</code>, or <code>ask</code>). The default policy is <code>allow</code>.</p> <pre><code>executor:\n  supervision: supervised\n  tool_policies:\n    # Simplest: tool name only (policy defaults to allow)\n    - tool: list_files\n\n    # Explicit policy with argument constraints\n    - tool: read_file\n      policy: allow\n      args:\n        - name: path\n          value: [/workspace/**]\n\n    # Allow reading only markdown files under /workspace\n    - tool: read_file\n      policy: allow\n      args:\n        - name: path\n          value: [\"/workspace/{*.md,**/*.md}\"]\n\n    # Deny specific patterns\n    - tool: write_file\n      policy: deny\n      args:\n        - name: path\n          value: [/etc/**]\n\n    # Arg-level policy override\n    - tool: execute_command\n      policy: allow\n      args:\n        - name: command\n          value: [ls]\n        - name: working_dir\n          value: [/sensitive/**]\n          policy: deny\n</code></pre>"},{"location":"installation/configuration/#policies","title":"Policies","text":"Policy Behavior <code>allow</code> Tool call is approved immediately (default) <code>deny</code> Tool call is denied immediately without consulting the user <code>ask</code> Tool call is forwarded to the user for interactive approval <p>When a rule has no <code>policy</code> field, it defaults to <code>allow</code>. When no rule matches a tool call, the call is forwarded to the user (same as <code>ask</code>).</p>"},{"location":"installation/configuration/#argument-matchers","title":"Argument Matchers","text":"<p>When a rule includes <code>args</code>, all matchers must pass for the rule to match. The <code>value</code> field is a list of patterns. How patterns are interpreted depends on the tool's argument type:</p> <ul> <li>Path arguments (e.g., <code>read_file.path</code>, <code>write_file.path</code>): patterns are glob-matched against the normalized path. Normalization prevents traversal attacks (e.g., <code>/workspace/../etc/passwd</code> is normalized to <code>/etc/passwd</code> and will not match <code>/workspace/**</code>).</li> <li>Shell command arguments (e.g., <code>execute_command.command</code>): patterns are shell-aware command patterns (see below).</li> <li>Other arguments: patterns are plain glob-matched against the raw string value.</li> </ul> Pattern Matches <code>/workspace/**</code> Any file at any depth under /workspace <code>/workspace/*/*.md</code> <code>.md</code> files exactly one directory under /workspace <code>/workspace/**/*.md</code> <code>.md</code> files at any depth under /workspace (not direct children) <code>/workspace/{*.md,**/*.md}</code> <code>.md</code> files at any depth including direct children <code>*.txt</code> Any .txt filename <code>/data/?</code> Single character under /data <code>*.{txt,md}</code> Files ending in .txt or .md <p>Note on <code>**</code> vs <code>**/*</code>: The pattern <code>/workspace/**</code> matches any file at any depth (including direct children). However, <code>/workspace/**/*.md</code> requires at least one intermediate directory \u2014 it will not match <code>/workspace/readme.md</code>. To match <code>.md</code> files at all depths including direct children, use the brace expansion pattern <code>/workspace/{*.md,**/*.md}</code>.</p> <p>Each argument matcher can also have its own <code>policy</code> field that overrides the tool-level policy. When multiple arg-level policies are present, the most restrictive one wins (<code>deny</code> &gt; <code>ask</code> &gt; <code>allow</code>).</p>"},{"location":"installation/configuration/#shell-aware-command-matching","title":"Shell-aware Command Matching","text":"<p>For <code>execute_command</code> and similar shell tools, the <code>command</code> argument is automatically evaluated with shell-aware matching. The shell string is parsed and decomposed into individual commands (handling <code>&amp;&amp;</code>, <code>||</code>, <code>;</code>, <code>|</code>, <code>$(...)</code>, backticks, subshells, and process substitutions). Every parsed command must match at least one pattern for the rule to match. If any command doesn't match, the rule falls through.</p> <p>There are two formats for defining command patterns: simple value patterns and structured command patterns.</p>"},{"location":"installation/configuration/#simple-value-patterns","title":"Simple Value Patterns","text":"<p>Each <code>value</code> pattern is interpreted as <code>\"executable [argPatterns...]\"</code>. Flags (arguments starting with <code>-</code>) are automatically skipped when matching positional arguments against patterns.</p> <ul> <li><code>ls</code> \u2014 allow <code>ls</code> with any arguments and any flags</li> <li><code>cat /workspace/**</code> \u2014 allow <code>cat</code> with any flags, but only with positional args matching <code>/workspace/**</code></li> <li><code>grep</code> \u2014 allow <code>grep</code> with any arguments</li> </ul> <pre><code>executor:\n  supervision: supervised\n  tool_policies:\n    # Whitelist safe commands \u2014 only these executables are allowed\n    - tool: execute_command\n      policy: allow\n      args:\n        - name: command\n          value:\n            - ls\n            - \"cat /workspace/**\"\n            - grep\n            - echo\n            - uname\n\n    # Explicitly deny dangerous commands\n    - tool: execute_command\n      policy: deny\n      args:\n        - name: command\n          value:\n            - rm\n            - dd\n            - mkfs\n</code></pre> <p>When argument patterns are provided (e.g., <code>cat /workspace/**</code>), every positional argument of the parsed command must match at least one pattern. Flags like <code>-n</code> or <code>--verbose</code> are skipped. Path arguments are normalized to prevent path traversal.</p>"},{"location":"installation/configuration/#structured-command-patterns","title":"Structured Command Patterns","text":"<p>For fine-grained control over flags and positional arguments, use the <code>commands</code> field instead of <code>value</code>. Each entry is a <code>CommandPattern</code> with:</p> Field Type Description <code>command</code> <code>string</code> Executable name to match (required) <code>allow_flags</code> <code>list&lt;string&gt;</code> Flag allowlist. <code>null</code> = any flags OK (default); <code>[]</code> = no flags allowed <code>deny_flags</code> <code>list&lt;string&gt;</code> Flag denylist. Checked first \u2014 takes precedence over <code>allow_flags</code> <code>paths</code> <code>list&lt;string&gt;</code> Glob patterns for path-like positional args (normalized before matching) <code>args</code> <code>list&lt;string&gt;</code> Glob patterns for non-path positional args (plain match, no normalization) <p>Positional arguments are classified as path-like (starts with <code>/</code>, <code>./</code>, <code>../</code>, <code>~</code>, or contains <code>/</code> without <code>://</code>) or value-like (everything else). Path-like args are matched against <code>paths</code> patterns; value-like args are matched against <code>args</code> patterns. If both <code>paths</code> and <code>args</code> are empty, any positional arguments are allowed \u2014 the constraint is only on the executable and flags.</p> <pre><code>executor:\n  supervision: supervised\n  tool_policies:\n    - tool: execute_command\n      args:\n        - name: command\n          commands:\n            # Path-only constraint, any flags\n            - command: cat\n              paths: [\"/workspace/**\"]\n\n            # Flag allowlist + path constraint\n            - command: grep\n              allow_flags: [\"-r\", \"-i\", \"-n\", \"-l\", \"-c\", \"-v\", \"-E\", \"-P\", \"-e\"]\n              paths: [\"/workspace/**\"]\n\n            # Flag denylist + mixed positional args\n            - command: chmod\n              deny_flags: [\"-R\", \"--recursive\"]\n              args: [\"*\"]               # permission value (644, +x, etc.)\n              paths: [\"/workspace/**\"]  # target path\n\n            # Flag-only restriction, any positional args\n            - command: rm\n              deny_flags: [\"-r\", \"-R\", \"-f\", \"--recursive\", \"--force\"]\n</code></pre> <p>Flag patterns support globs (e.g., <code>-*f*</code> matches <code>-rf</code>, <code>-force</code>). Combined flags like <code>-rf</code> are matched as-is \u2014 list all relevant forms explicitly or use globs.</p>"},{"location":"installation/configuration/#remote-worker","title":"Remote Worker","text":"<p>Start a lightweight worker process to handle tool execution:</p> <pre><code>fraggle worker [--port PORT] [--work-dir DIR]\n</code></pre> Option Description Default <code>--port</code> HTTP port to listen on <code>9292</code> <code>--work-dir</code> Working directory for tools <code>./data/workspace</code> <p>The worker exposes two endpoints:</p> Endpoint Method Description <code>/health</code> GET Returns a JSON array of tool names <code>/api/v1/tools/execute</code> POST Execute a tool and return the result <p>The worker loads the same built-in tools as the main process but runs with no supervision and no remote forwarding \u2014 it always executes locally.</p>"},{"location":"installation/configuration/#agent","title":"Agent","text":"<p>Controls agent behavior during the ReAct loop:</p> Option Description Default <code>temperature</code> LLM sampling temperature <code>0.7</code> <code>max_tokens</code> Maximum response tokens <code>4096</code> <code>max_iterations</code> Maximum tool-use cycles per request <code>10</code> <code>max_history_messages</code> Messages to include in context <code>20</code> <code>vision</code> Pass images from chat messages to the LLM for vision analysis (requires a vision-capable model) <code>false</code>"},{"location":"installation/configuration/#registered-chats","title":"Registered Chats","text":"<p>Pre-configure specific chats with custom settings:</p> <pre><code>chats:\n  registered:\n    - id: \"group-abc123\"        # Signal group ID\n      name: \"Dev Team\"          # Human-readable name\n      trigger_override: \"@bot\"  # Custom trigger for this chat\n      enabled: true             # Enable/disable this chat\n</code></pre>"},{"location":"installation/configuration/#playwright-optional","title":"Playwright (Optional)","text":"<p>For JavaScript-heavy websites, configure Playwright for full browser rendering:</p> <pre><code>web:\n  playwright:\n    ws_endpoint: ws://localhost:3000\n    navigation_timeout: 30000     # Page load timeout (ms)\n    wait_after_load: 2000         # Wait for JS to settle (ms)\n    viewport_width: 1280\n    viewport_height: 720\n    user_agent: null              # Custom user agent\n</code></pre> <p>See Tools - Web Tools for setup instructions.</p>"},{"location":"installation/configuration/#skills","title":"Skills","text":"<p>Controls discovery and loading of agent skills \u2014 portable, <code>agentskills.io</code>-compliant capability bundles authored as <code>SKILL.md</code> files with YAML frontmatter. Only skill metadata is injected into the system prompt at startup; the model reads the full body on demand via <code>read_file</code>.</p> Option Description Default <code>enabled</code> Whether skills are loaded and advertised to the agent <code>true</code> <code>skills_dir</code> Project-scoped directory containing skill bundles <code>./config/skills</code> <code>global_dir</code> Cross-project skills directory (<code>null</code> to disable) <code>~/.fraggle/skills</code> <code>extra_paths</code> Additional files or directories with <code>EXPLICIT</code> precedence (highest) <code>[]</code> <code>enable_slash_commands</code> Enable explicit <code>/skill:name</code> invocation in chat bridges <code>true</code> <p>Precedence on name collision (higher wins): <code>PACKAGE &lt; GLOBAL &lt; PROJECT &lt; EXPLICIT</code>. You can ship defaults globally and override per-project without editing the original.</p> <pre><code>skills:\n  enabled: true\n  skills_dir: ./config/skills\n  global_dir: ~/.fraggle/skills\n  extra_paths:\n    - ./vendor/shared-skills\n  enable_slash_commands: true\n</code></pre> <p>See Skills for the skill format, authoring guide, and invocation semantics.</p>"},{"location":"installation/configuration/#tracing","title":"Tracing","text":"<p>Controls agent tracing and observability. When enabled, trace events are captured during agent execution and viewable via the API and dashboard.</p> Option Description Default <code>level</code> Tracing level: <code>off</code>, <code>metadata</code>, <code>full</code> <code>off</code> <p>Tracing levels:</p> <ul> <li><code>off</code> \u2014 Tracing is completely disabled. No trace events are captured and no overhead is added.</li> <li><code>metadata</code> \u2014 Trace events are captured (agent lifecycle, tool calls, LLM call timing) but LLM message content is omitted from <code>detail</code> fields.</li> <li><code>full</code> \u2014 Everything is captured, including full LLM request/response content with message payloads and token usage.</li> </ul> <pre><code>tracing:\n  level: full\n</code></pre>"},{"location":"installation/configuration/#api-server","title":"API Server","text":"<p>Enable the REST API for external integrations:</p> <pre><code>api:\n  enabled: true\n  host: \"0.0.0.0\"\n  port: 9191\n  cors:\n    enabled: true\n    allowed_origins:\n      - \"http://localhost:3000\"\n      - \"https://myapp.example.com\"\n</code></pre>"},{"location":"installation/configuration/#dashboard","title":"Dashboard","text":"<p>Enable the web dashboard UI (requires API to be enabled):</p> <pre><code>api:\n  enabled: true\ndashboard:\n  enabled: true\n  static_path: null  # Use embedded assets\n</code></pre>"},{"location":"installation/discord-setup/","title":"Discord Setup","text":"<p>This guide covers setting up Discord integration for Fraggle.</p>"},{"location":"installation/discord-setup/#overview","title":"Overview","text":"<p>Fraggle's Discord integration is designed for personal use - the bot is installed directly to your Discord user account, not to servers. This means:</p> <ul> <li>You communicate with your bot instance privately via DMs</li> <li>Your bot is not shared with other users</li> <li>No server administrator permissions are required</li> <li>Your conversations remain private</li> </ul> <p>Server Installation</p> <p>Multi-user/server installation is not currently supported. Each user runs their own Fraggle instance with their own bot.</p>"},{"location":"installation/discord-setup/#prerequisites","title":"Prerequisites","text":"<ul> <li>A Discord account</li> <li>A Discord application with a bot token (see below for instructions)</li> </ul>"},{"location":"installation/discord-setup/#creating-a-discord-bot","title":"Creating a Discord Bot","text":""},{"location":"installation/discord-setup/#step-1-create-an-application","title":"Step 1: Create an Application","text":"<ol> <li>Go to the Discord Developer Portal</li> <li>Click \"New Application\"</li> <li>Enter a name for your bot (e.g., \"My Fraggle Bot\")</li> <li>Accept the Terms of Service and click \"Create\"</li> </ol>"},{"location":"installation/discord-setup/#step-2-configure-the-bot","title":"Step 2: Configure the Bot","text":"<ol> <li>In your application, go to the \"Bot\" section in the left sidebar</li> <li>Click \"Reset Token\" to generate a new token</li> <li>Copy and save the token - you won't be able to see it again!</li> <li>Under \"Privileged Gateway Intents\", enable:<ul> <li>Message Content Intent (required to read message text)</li> </ul> </li> </ol> <p>Keep Your Token Secret</p> <p>Never share your bot token or commit it to version control. Anyone with your token can control your bot.</p>"},{"location":"installation/discord-setup/#step-3-configure-oauth2-required-for-dms","title":"Step 3: Configure OAuth2 (Required for DMs)","text":"<p>To enable DM conversations, you need to configure OAuth2 so Fraggle can send you an initial welcome message.</p> <ol> <li>Go to the \"OAuth2\" section in the left sidebar</li> <li>Under \"Client Information\":<ul> <li>Copy your Client ID (also visible at the top of the General Information page)</li> <li>Click \"Reset Secret\" and copy your Client Secret</li> </ul> </li> <li>Under \"Redirects\", add your callback URL:<ul> <li>For local development: <code>http://localhost:9191/api/v1/discord/oauth/callback</code></li> <li>For production: <code>https://your-domain/api/v1/discord/oauth/callback</code></li> </ul> </li> </ol> <p>!!! note Only the Fraggle API is required for Discord support,     the Dashboard does not need to be enabled.</p> <p>Keep Your Client Secret Safe</p> <p>Like the bot token, never share your client secret or commit it to version control.</p>"},{"location":"installation/discord-setup/#step-4-configure-installation-settings","title":"Step 4: Configure Installation Settings","text":"<ol> <li>Go to the \"Installation\" section in the left sidebar</li> <li>Under \"Installation Contexts\", configure for user installation:<ul> <li>\u2705 Enable \"User Install\"</li> <li>\u274c Disable \"Guild Install\" (optional, but recommended for personal use)</li> </ul> </li> <li>Under \"Default Install Settings\":<ul> <li>For \"User Install\", add the scopes:<ul> <li><code>applications.commands</code> (Added by default)</li> </ul> </li> </ul> </li> </ol>"},{"location":"installation/discord-setup/#step-5-install-the-bot-via-oauth2","title":"Step 5: Install the Bot via OAuth2","text":"<ol> <li>Start Fraggle with your Discord configuration (see below)</li> <li>Navigate to the Dashboard Chat Bridges section or open the <code>http://localhost:9191/api/v1/discord/oauth/authorize</code> API endpoint</li> <li>Discord will redirect you to authorize the application</li> <li>Select \"Install to your account\"</li> <li>Click \"Authorize\"</li> <li>You'll be redirected back to Fraggle, and a welcome DM will be sent to you</li> </ol> <p>You will receive a DM from your bot telling you everything is complete.</p>"},{"location":"installation/discord-setup/#configuration","title":"Configuration","text":"<p>Add Discord configuration to your <code>fraggle.yaml</code>:</p> <pre><code>fraggle:\n  bridges:\n    discord:\n      enabled: true\n      token: \"your-bot-token-here\"           # From Step 2 above\n      client_secret: \"your-client-secret\"    # From Step 3 (OAuth2)\n      oauth_redirect_uri: \"http://localhost:9191/api/v1/discord/oauth/callback\"\n      trigger: \"!fraggle\"                    # Trigger prefix (optional for DMs)\n      respond_to_direct_messages: true\n      show_typing_indicator: true\n      max_images_per_message: 10             # Discord allows up to 10\n      max_file_size_mb: 10                   # 10 free, 50 Nitro Basic, 500 Nitro\n</code></pre>"},{"location":"installation/discord-setup/#configuration-options","title":"Configuration Options","text":"Option Description Default <code>token</code> Discord bot token (required) - <code>enabled</code> Whether Discord bridge is active <code>true</code> if token is set <code>client_id</code> OAuth2 client ID (extracted from token if not set) - <code>client_secret</code> OAuth2 client secret (required for DM setup) - <code>oauth_redirect_uri</code> OAuth2 callback URL (must match Developer Portal) - <code>trigger</code> Prefix to trigger bot (null = respond to all) <code>!fraggle</code> <code>respond_to_direct_messages</code> Respond to DMs without trigger <code>true</code> <code>show_typing_indicator</code> Show typing indicator while processing <code>true</code> <code>max_images_per_message</code> Maximum images per message (Discord limit: 10) <code>10</code> <code>max_file_size_mb</code> Max file size in MB (see limits below) <code>10</code> <code>allowed_channel_ids</code> Only respond in these channels (empty = all) <code>[]</code> <code>allowed_guild_ids</code> Only respond in these guilds (empty = all) <code>[]</code>"},{"location":"installation/discord-setup/#file-size-limits","title":"File Size Limits","text":"<p>Discord has different file upload limits based on your subscription:</p> Plan Max File Size Free 10 MB Nitro Basic 50 MB Nitro 500 MB <p>Set <code>max_file_size_mb</code> according to your Discord subscription level.</p>"},{"location":"installation/discord-setup/#using-your-bot","title":"Using Your Bot","text":""},{"location":"installation/discord-setup/#direct-messages","title":"Direct Messages","text":"<p>Once the OAuth2 flow is complete, your bot appears in your Discord DM list. Simply send a message:</p> <pre><code>Hello! What can you help me with?\n</code></pre> <p>When <code>respond_to_direct_messages</code> is <code>true</code> (the default), your bot responds to all DMs automatically without needing a trigger prefix.</p>"},{"location":"installation/discord-setup/#using-a-trigger-prefix","title":"Using a Trigger Prefix","text":"<p>If you prefer to use a trigger prefix even in DMs:</p> <pre><code>fraggle:\n  bridges:\n    discord:\n      respond_to_direct_messages: false\n      trigger: \"!ask\"\n</code></pre> <p>Then message your bot with:</p> <pre><code>!ask What's the weather like today?\n</code></pre>"},{"location":"installation/discord-setup/#sending-images","title":"Sending Images","text":"<p>Fraggle supports sending images in Discord messages. You can include images in two ways:</p>"},{"location":"installation/discord-setup/#inline-image-syntax","title":"Inline Image Syntax","text":"<p>Use the <code>[[image:URL]]</code> syntax to include images directly in responses:</p> <pre><code>Here's a cute cat picture: [[image:https://example.com/cat.jpg]]\n</code></pre> <p>Discord supports up to 10 images per message, and Fraggle will automatically combine them.</p>"},{"location":"installation/discord-setup/#screenshot-tool","title":"Screenshot Tool","text":"<p>Use the <code>screenshot_page</code> tool (requires Playwright) to capture and send website screenshots:</p> <pre><code>Can you show me what google.com looks like?\n</code></pre>"},{"location":"installation/discord-setup/#text-formatting","title":"Text Formatting","text":"<p>Discord supports rich markdown formatting. The LLM can use:</p> Syntax Result <code>**bold**</code> Bold <code>*italic*</code> Italic <code>~~strikethrough~~</code> ~~Strikethrough~~ <code>\\|\\|spoiler\\|\\|</code> Spoiler (hidden until clicked) <code>`code`</code> <code>Inline code</code> <code>```code```</code> Code block <code>&gt; quote</code> Block quote <code># Heading</code> Large heading <code>- item</code> Bullet list"},{"location":"installation/discord-setup/#troubleshooting","title":"Troubleshooting","text":""},{"location":"installation/discord-setup/#invalid-bot-token","title":"\"Invalid bot token\"","text":"<ol> <li>Verify the token is correct in your <code>fraggle.yaml</code></li> <li>Make sure you copied the full token from the Developer Portal</li> <li>Try resetting the token and using the new one</li> </ol>"},{"location":"installation/discord-setup/#message-content-intent-not-enabled","title":"\"Message Content Intent not enabled\"","text":"<ol> <li>Go to your bot's settings in the Discord Developer Portal</li> <li>Navigate to the \"Bot\" section</li> <li>Enable \"Message Content Intent\" under Privileged Gateway Intents</li> <li>Save changes and restart Fraggle</li> </ol>"},{"location":"installation/discord-setup/#bot-appears-offline","title":"Bot appears offline","text":"<ol> <li>Check that Fraggle is running with Discord bridge enabled</li> <li>Verify the token is valid</li> <li>Check the logs for connection errors: <code>$FRAGGLE_ROOT/logs/fraggle.log</code></li> </ol>"},{"location":"installation/discord-setup/#bot-doesnt-respond-to-messages","title":"Bot doesn't respond to messages","text":"<ol> <li>Ensure <code>respond_to_direct_messages: true</code> is set (for DMs)</li> <li>Check if a trigger prefix is required and you're using it</li> <li>Verify the bot is installed to your user account (check DM list)</li> <li>Check logs for any errors during message processing</li> </ol>"},{"location":"installation/discord-setup/#bot-not-appearing-in-dms","title":"Bot not appearing in DMs","text":"<p>User-installed Discord apps don't automatically appear in your DM list. You need to complete the OAuth2 flow:</p> <ol> <li>Verify <code>client_secret</code> and <code>oauth_redirect_uri</code> are set in your config</li> <li>Make sure the redirect URI in your config matches the one in Discord Developer Portal</li> <li>Visit <code>http://localhost:9191/api/v1/discord/oauth/authorize</code></li> <li>Complete the authorization flow</li> <li>Your bot should send you a welcome DM</li> </ol>"},{"location":"installation/discord-setup/#oauth2-errors","title":"OAuth2 errors","text":"<p>\"Discord OAuth not configured\"</p> <ul> <li>Ensure <code>client_secret</code> and <code>oauth_redirect_uri</code> are set in your <code>fraggle.yaml</code></li> <li>Restart Fraggle after changing configuration</li> </ul> <p>\"Failed to exchange authorization code\"</p> <ul> <li>Verify your <code>client_secret</code> is correct</li> <li>Check that the redirect URI matches exactly (including trailing slashes)</li> </ul> <p>\"Discord bot not connected\"</p> <ul> <li>The bot must be running and connected before completing OAuth2</li> <li>Check that your bot token is valid</li> <li>Look for connection errors in the logs</li> </ul>"},{"location":"installation/discord-setup/#file-too-large-errors","title":"\"File too large\" errors","text":"<p>Discord limits file uploads based on your subscription:</p> <ul> <li>Free users: 10 MB max</li> <li>Nitro Basic: 50 MB max</li> <li>Nitro: 500 MB max</li> </ul> <p>Update your configuration to match:</p> <pre><code>fraggle:\n  bridges:\n    discord:\n      max_file_size_mb: 50  # For Nitro Basic\n</code></pre>"},{"location":"installation/discord-setup/#security-considerations","title":"Security Considerations","text":"<ul> <li>Never share your bot token or client secret - anyone with these credentials can control your bot</li> <li>Store tokens and secrets securely (e.g., environment variables, secrets manager)</li> <li>The bot can read all messages in conversations it's part of</li> <li>Consider running Fraggle as a dedicated system user</li> <li>The OAuth2 redirect URI should use HTTPS in production environments</li> <li>Review Discord's Terms of Service and Developer Terms</li> </ul>"},{"location":"installation/getting-started/","title":"Getting Started","text":"<p>This guide walks you through setting up Fraggle from scratch.</p>"},{"location":"installation/getting-started/#requirements","title":"Requirements","text":"<ul> <li>JDK 21+ - Fraggle requires Java 21 or later</li> <li>LLM Provider - One of:<ul> <li>LM Studio (local, recommended for development)</li> <li>OpenAI API key</li> <li>Anthropic API key</li> </ul> </li> <li>signal-cli - Optional, for Signal chat bridge (installation guide)</li> </ul>"},{"location":"installation/getting-started/#installation","title":"Installation","text":""},{"location":"installation/getting-started/#1-download-the-latest-release","title":"1. Download the Latest Release","text":"<pre><code>wget https://github.com/DrewCarlson/Fraggle/releases/latest/download/fraggle.zip\nunzip fraggle.zip\ncd fraggle\n</code></pre>"},{"location":"installation/getting-started/#2-configure-fraggle","title":"2. Configure Fraggle","text":"<p>Run the interactive configuration wizard:</p> <pre><code>./bin/fraggle configure\n</code></pre> <p>This walks you through setting up:</p> <ul> <li>LLM provider (LM Studio, OpenAI, or Anthropic)</li> <li>Signal integration (optional)</li> <li>API server and dashboard (optional)</li> </ul> <p>Alternatively, copy and edit the example configuration manually:</p> <pre><code>cp config/fraggle.example.yaml config/fraggle.yaml\n</code></pre> <p>See the Configuration guide for all options.</p>"},{"location":"installation/getting-started/#3-test-with-interactive-mode","title":"3. Test with Interactive Mode","text":"<p>Before setting up chat bridges, test that everything works:</p> <pre><code>./bin/fraggle chat\n</code></pre> <p>This starts an interactive chat session where you can test the agent directly.</p>"},{"location":"installation/getting-started/#running-modes","title":"Running Modes","text":""},{"location":"installation/getting-started/#interactive-chat-mode","title":"Interactive Chat Mode","text":"<p>Test the agent without chat bridges:</p> <pre><code># With default config\n./bin/fraggle chat\n\n# With custom config file\n./bin/fraggle chat -c /path/to/config.yaml\n\n# With model override\n./bin/fraggle chat -m gpt-4\n</code></pre>"},{"location":"installation/getting-started/#full-service-mode","title":"Full Service Mode","text":"<p>Run with chat bridges enabled:</p> <pre><code># With default config\n./bin/fraggle run\n\n# With custom config file\n./bin/fraggle run -c /path/to/config.yaml\n</code></pre>"},{"location":"installation/getting-started/#custom-installation-location","title":"Custom Installation Location","text":"<p>To install Fraggle in a specific location, set <code>FRAGGLE_ROOT</code>:</p> <pre><code>export FRAGGLE_ROOT=/opt/fraggle\n./bin/fraggle run\n</code></pre>"},{"location":"installation/getting-started/#directory-structure","title":"Directory Structure","text":"<p>When running, Fraggle creates this directory structure under <code>FRAGGLE_ROOT</code>:</p> <pre><code>$FRAGGLE_ROOT/\n\u251c\u2500\u2500 config/\n\u2502   \u251c\u2500\u2500 fraggle.yaml        # Main configuration\n\u2502   \u2514\u2500\u2500 prompts/            # Prompt templates\n\u2502       \u251c\u2500\u2500 SYSTEM.md       # System prompt\n\u2502       \u251c\u2500\u2500 IDENTITY.md     # Agent identity/personality\n\u2502       \u2514\u2500\u2500 USER.md         # User-specific instructions\n\u251c\u2500\u2500 data/\n\u2502   \u251c\u2500\u2500 apps/               # Auto-installed applications (signal-cli, etc.)\n\u2502   \u251c\u2500\u2500 memory/             # Conversation memory storage\n\u2502   \u2514\u2500\u2500 workspace/          # Sandbox working directory\n\u2514\u2500\u2500 logs/\n    \u2514\u2500\u2500 fraggle.log         # Application logs (daily rotation)\n</code></pre>"},{"location":"installation/getting-started/#next-steps","title":"Next Steps","text":"<ul> <li>Configuration Reference - Full configuration options</li> <li>Signal Setup - Connect to Signal messenger</li> <li>Tools - Available tools and custom tool development</li> </ul>"},{"location":"installation/signal-setup/","title":"Signal Setup","text":"<p>This guide covers setting up Signal integration for Fraggle.</p>"},{"location":"installation/signal-setup/#prerequisites","title":"Prerequisites","text":"<ul> <li>A phone number to register with Signal</li> <li>signal-cli (automatically installed by Fraggle, or manually installed)</li> </ul>"},{"location":"installation/signal-setup/#installing-signal-cli","title":"Installing signal-cli","text":""},{"location":"installation/signal-setup/#automatic-installation-recommended","title":"Automatic Installation (Recommended)","text":"<p>Fraggle can automatically download and install signal-cli when it's not found in your system PATH. This is enabled by default and requires no manual setup.</p> <p>When you first run Fraggle with Signal bridge enabled, it will:</p> <ol> <li>Check if signal-cli is available in your system PATH</li> <li>If not found, download the appropriate version to <code>data/apps/signal-cli-{version}/</code></li> <li>On Linux: Downloads the native binary for better performance</li> <li>On macOS/Windows: Downloads the Java-based version</li> </ol> <p>You can control this behavior with the <code>auto_install</code> and <code>signal_cli_version</code> configuration options.</p>"},{"location":"installation/signal-setup/#manual-installation","title":"Manual Installation","text":"<p>If you prefer to install signal-cli manually, or if automatic installation fails:</p>"},{"location":"installation/signal-setup/#macos-homebrew","title":"macOS (Homebrew)","text":"<pre><code>brew install signal-cli\n</code></pre>"},{"location":"installation/signal-setup/#linux-native-binary","title":"Linux (Native Binary)","text":"<p>Download the native binary for better performance:</p> <pre><code># Example for version 0.14.2\nwget https://github.com/AsamK/signal-cli/releases/download/v0.14.2/signal-cli-0.14.2-Linux-native.tar.gz\ntar xf signal-cli-0.14.2-Linux-native.tar.gz\nsudo mv signal-cli /usr/local/bin/signal-cli\n</code></pre>"},{"location":"installation/signal-setup/#linuxmacos-java-version","title":"Linux/macOS (Java Version)","text":"<p>If you prefer the Java-based version:</p> <pre><code># Example for version 0.14.2\nwget https://github.com/AsamK/signal-cli/releases/download/v0.14.2/signal-cli-0.14.2.tar.gz\ntar xf signal-cli-0.14.2.tar.gz\nsudo mv signal-cli-0.14.2 /opt/signal-cli\nsudo ln -s /opt/signal-cli/bin/signal-cli /usr/local/bin/signal-cli\n</code></pre>"},{"location":"installation/signal-setup/#registering-a-phone-number","title":"Registering a Phone Number","text":""},{"location":"installation/signal-setup/#option-1-new-registration","title":"Option 1: New Registration","text":"<p>Register a new phone number with Signal:</p> <pre><code># Request verification code via SMS\nsignal-cli -u +1234567890 register\n\n# Or request via voice call\nsignal-cli -u +1234567890 register --voice\n\n# Verify with the code you received\nsignal-cli -u +1234567890 verify CODE\n</code></pre>"},{"location":"installation/signal-setup/#option-2-link-to-existing-account","title":"Option 2: Link to Existing Account","text":"<p>Link signal-cli as a secondary device to an existing Signal account:</p> <pre><code>signal-cli link -n \"Fraggle Bot\"\n</code></pre> <p>This displays a QR code or URI. Scan it with the Signal app on your phone:</p> <ol> <li>Open Signal on your phone</li> <li>Go to Settings &gt; Linked Devices</li> <li>Tap \"Link New Device\"</li> <li>Scan the QR code</li> </ol>"},{"location":"installation/signal-setup/#configuration","title":"Configuration","text":"<p>Add Signal configuration to your <code>fraggle.yaml</code>:</p> <pre><code>fraggle:\n  bridges:\n    signal:\n      enabled: true\n      phone: \"+1234567890\"            # Your registered number\n      config_dir: ~/.config/fraggle/signal\n      trigger: \"@fraggle\"             # Trigger for group messages\n      respond_to_direct_messages: true\n      show_typing_indicator: true\n      # Auto-installation settings (optional)\n      auto_install: true              # Auto-download signal-cli if not found\n      signal_cli_version: \"0.14.2\"   # Version to download\n</code></pre> <p>Automatic Installation</p> <p>With <code>auto_install: true</code> (the default), you don't need to install signal-cli manually. Fraggle will download it automatically on first run if it's not found in your system PATH.</p>"},{"location":"installation/signal-setup/#configuration-options","title":"Configuration Options","text":"Option Description Default <code>phone</code> Registered Signal phone number (with country code) Required <code>enabled</code> Whether Signal bridge is active <code>true</code> if phone is set <code>config_dir</code> Directory for Signal configuration <code>~/.config/fraggle/signal</code> <code>trigger</code> Prefix to trigger bot in group chats <code>@fraggle</code> <code>signal_cli_path</code> Path to signal-cli (null = use PATH or auto-install) <code>null</code> <code>auto_install</code> Automatically download signal-cli if not found <code>true</code> <code>signal_cli_version</code> Version of signal-cli to auto-install <code>0.14.2</code> <code>respond_to_direct_messages</code> Respond to DMs without trigger <code>true</code> <code>show_typing_indicator</code> Show typing indicator while processing <code>true</code>"},{"location":"installation/signal-setup/#trigger-behavior","title":"Trigger Behavior","text":""},{"location":"installation/signal-setup/#direct-messages","title":"Direct Messages","text":"<p>When <code>respond_to_direct_messages</code> is <code>true</code>, Fraggle responds to all direct messages automatically.</p>"},{"location":"installation/signal-setup/#group-messages","title":"Group Messages","text":"<p>In group chats, Fraggle only responds when the message starts with the trigger prefix:</p> <pre><code>@fraggle what's the weather like?\n</code></pre> <p>Set <code>trigger</code> to <code>null</code> to respond to all group messages (use with caution).</p>"},{"location":"installation/signal-setup/#per-chat-triggers","title":"Per-Chat Triggers","text":"<p>You can override the trigger for specific chats:</p> <pre><code>fraggle:\n  bridges:\n    signal:\n      trigger: \"@fraggle\"\n  chats:\n    registered:\n      - id: \"group-abc123\"\n        name: \"Dev Team\"\n        trigger_override: \"@bot\"    # Use @bot in this group\n</code></pre>"},{"location":"installation/signal-setup/#text-formatting","title":"Text Formatting","text":"<p>Fraggle supports Signal's text formatting. The LLM can use markdown-like syntax that's automatically converted:</p> Syntax Result <code>**bold**</code> Bold <code>*italic*</code> Italic <code>~~strikethrough~~</code> ~~Strikethrough~~ <code>\\|\\|spoiler\\|\\|</code> Spoiler (hidden until tapped) <code>`monospace`</code> <code>Monospace</code> <p>Unsupported Formatting</p> <p>Markdown links (<code>[text](url)</code>) and images (<code>![alt](url)</code>) are NOT supported in Signal messages.</p>"},{"location":"installation/signal-setup/#troubleshooting","title":"Troubleshooting","text":""},{"location":"installation/signal-setup/#failed-to-connect-to-signal","title":"\"Failed to connect to Signal\"","text":"<ol> <li>Verify signal-cli is installed: <code>signal-cli --version</code></li> <li>Check the phone number is registered: <code>signal-cli -u +1234567890 receive</code></li> <li>Ensure <code>config_dir</code> exists and is writable</li> </ol>"},{"location":"installation/signal-setup/#messages-not-being-received","title":"\"Messages not being received\"","text":"<ol> <li>Run in interactive mode first to verify the agent works: <code>./bin/fraggle chat</code></li> <li>Check logs at <code>$FRAGGLE_ROOT/logs/fraggle.log</code></li> <li>Try receiving messages manually: <code>signal-cli -u +1234567890 receive</code></li> </ol>"},{"location":"installation/signal-setup/#permission-denied-errors","title":"\"Permission denied\" errors","text":"<p>Ensure the user running Fraggle has:</p> <ul> <li>Read/write access to <code>config_dir</code></li> <li>Execute permission on <code>signal-cli</code></li> </ul>"},{"location":"installation/signal-setup/#auto-installation-issues","title":"Auto-installation issues","text":"<p>If automatic installation of signal-cli fails:</p> <ol> <li>Check internet connectivity - Fraggle needs to download from GitHub releases</li> <li>Verify write permissions to <code>data/apps/</code> directory</li> <li>Check logs for specific download or extraction errors</li> <li>On Linux, ensure <code>tar</code> command is available for extraction</li> <li>Try manual installation instead (see above)</li> </ol> <p>To disable auto-installation and use a manually installed version:</p> <pre><code>fraggle:\n  bridges:\n    signal:\n      auto_install: false\n      signal_cli_path: /path/to/signal-cli  # Optional: specify explicit path\n</code></pre>"},{"location":"installation/signal-setup/#updating-signal-cli-version","title":"Updating signal-cli version","text":"<p>To use a different version of signal-cli:</p> <pre><code>fraggle:\n  bridges:\n    signal:\n      signal_cli_version: \"0.13.24\"  # Specify desired version\n</code></pre> <p>Fraggle will download the new version on next startup. Old versions in <code>data/apps/</code> can be manually deleted.</p>"},{"location":"installation/signal-setup/#security-considerations","title":"Security Considerations","text":"<ul> <li>The Signal configuration directory contains sensitive authentication data</li> <li>Restrict access to <code>config_dir</code> (e.g., <code>chmod 700</code>)</li> <li>Consider running Fraggle as a dedicated user</li> <li>The bot can access messages in any chat it's added to - be mindful of group memberships</li> </ul>"}]}